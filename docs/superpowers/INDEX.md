# Savig SDD Artifacts — Index

Spec-driven-development trail for Savig. Each feature flows
**brainstorm → design spec (`specs/`) → implementation plan(s) (`plans/`) → execute & merge**.
Plans use `- [ ]` checkboxes for task tracking — boxes are intentionally left unchecked
in-file. Every cycle runs a `feature-dev:code-reviewer` pass (often a review LOOP until no
Critical/Important remain) before a `--no-ff` merge to `main`.

Files are dated `YYYY-MM-DD` by authoring date. **All work below is merged to `main`.**
This index is a navigation aid — open the individual file for full detail; don't
consolidate these into one document (it would destroy the dated provenance). Run
`ls specs/` / `ls plans/` for exact filenames; paths below are abbreviated with `…`.

## Status at a glance (2026-06-28)

> **Verified health (2026-06-28):** `pnpm test` → **1525 unit pass (96 files)**; **106 e2e** (65 specs); `tsc --noEmit` clean. M4 is COMPLETE; only optional polish + principled deferrals remain (see [What's next / backlog](#whats-next--backlog)).

| Milestone | Scope | Status |
|-----------|-------|--------|
| **M1** | Core editor (engine, services, UI, audio clock) | ✅ COMPLETE |
| **M2** | Vector drawing tools (pen/shapes/brush) + a large polish program | ✅ COMPLETE (slices 1–35) |
| **M3** | Path morphing & advanced tweens | ✅ COMPLETE — every feature was pulled forward into M2 |
| **M4** | Grouping, layers & nested symbols/clips | ✅ COMPLETE — multi-object toolkit (36–44) + grouping phase 1 (42) done; **grouping COMPLETE (45a–45f)**; **boolean ops COMPLETE (46)**; **NESTED SYMBOLS COMPLETE (47a foundation + 47b instance-transform-UI + 47-edit edit-mode + 47c independent-timelines + 47d library)** — `SymbolAsset` + recursive `flattenInstances` walker; create-symbol; instance bbox/scale/rotate handles + move-snapping; edit-in-place (`editPath` focused-scene); per-instance `symbolTime` remap (two instances diverge in frame); and a symbols library (place/swap/count + authoring cycle guard). ALL M4 headline features shipped. **IN-SYMBOL EDITING SURFACE FULLY CLOSED** (author-in-symbol phases 1–9 + general in-symbol timeline keyframe editing). **47d SYMBOL/ASSET LIBRARY FEATURE-COMPLETE; 47c PER-INSTANCE TIMING scalar-surface COMPLETE (duration-override + ping-pong + play-count-N + random-start phase + symbol-instance internal animation in computeProjectDuration); 47b polish — multi-select bounds, per-object dashed outline, multi-select MOVE preview all handle groups/instances; LAYOUT — boolean-op keyboard shortcuts (Cmd/Ctrl+Shift+U/S/I/E) + center-on-canvas + distribute-by-centers shipped.** Remaining M4: **47c KEYFRAMED time-remap DONE** (`e6e797c`+`9b8abcd`; direct `symbolTimeTrack` curve — 47c fully complete), **group LOCK cascade DONE** (`841475c`), instance-in-group drag-preview (fiddly), boolean-ops follow-ups, non-grouping layout finishers (~~spacing input~~ `35d08ae` / ~~edge-align-to-artboard~~ `8f50828` DONE; paste-at-cursor / snapping handles remain) |
| **M5** | **Agent Authoring & Headless Render** (headless core · raster · DSL · MCP · video · macros · templates · scenes · text) | 🚧 IN PROGRESS — **slices 1–4 + 6–7 DONE — END-TO-END AGENT-DRIVEABLE**: (1) headless `src/core` builders + describe + validate; (2) render-to-raster (the agent's eyes); (3) declarative `ShortDoc` DSL; (4) **stdio MCP server** (`src/mcp`, 14 tools returning describe + thumbnail — Claude drives Savig directly); (6) semantic macros; (7) 5 example-short templates (few-shot corpus); (5) animated **GIF export**; (9) **text primitive**; (8a) **camera** (animatable pan/zoom/roll, export+runtime+headless render). **Functionally complete** — only 8b (multi-scene sequencing) deferred to its own model-redesign spec. Pulls forward roadmap M8 + M9 + part of M7. → [§Milestone 5](#milestone-5--agent-authoring--headless-render-in-progress) |
| M6–M11 | CSS export · multitrack audio · scenes(partial→M5) · cloud · collab | ⬜ Not started (master spec §10) |

> **M3 note:** M3's deliverables (interpolate path `d` between keyframes; motion paths;
> custom-bezier easing UI) all shipped during M2 — path morphing (slice 3), the
> arc-length / node-correspondence / per-node-easing morph roadmap (F1–F4), motion paths
> (slice 5). There is no separate M3 work to do.

## Milestone 1 — Core editor (COMPLETE)

| Area | Spec | Plan(s) | Merge |
|------|------|---------|-------|
| Master design | `specs/2026-06-19-savig-animated-svg-editor-design.md` | — | — |
| Animation engine | ↑ | `plans/2026-06-19-savig-engine-core.md` | `86a9974` |
| Services layer | ↑ | `plans/2026-06-19-savig-services.md` | `04c6271` |
| UI layer | ↑ | `plans/2026-06-19-savig-ui.md` | `2e7d2fc` (completes M1) |
| Audio master clock | ↑ (§4) | — | `c0cd9f0` |

## Milestone 2 — Vector drawing tools (COMPLETE, slices 1–35)

Core authoring (1–8), the morph/easing roadmap (F1–F4), then a long sequence of
feature + polish slices. `…` = `YYYY-MM-DD-savig-m2-` matching the slice's date.

| Slice / feature | Spec | Merge |
|-----------------|------|-------|
| 1 — Vector foundation | `specs/…vector-foundation-design.md` | `56cbc22`, `63baf40` |
| 2 — Pen / bezier paths + node editing | `specs/…slice2-pen-paths-design.md` | `b2ab484` |
| 3 — Path morphing | `specs/…slice3-path-morphing-design.md` | `5a3cff9` |
| Morph/easing roadmap | `specs/…morph-easing-roadmap-design.md` | `dd2d3e3` |
| F1 — Keyframe easing UI (custom bezier) | `specs/…keyframe-easing-ui-design.md` | `dd2d3e3` |
| F2 — Arc-length / cross-shape morph | `specs/…arc-length-morph-design.md` | `c368e0b`, `f408905` |
| F3 — Node correspondence editor | `specs/…node-correspondence-design.md` | `97e50f7`, `72c0f07`, `5e17f56` (+ `a6de8b9`) |
| F4 — Per-node easing | `specs/…per-node-easing-design.md` | `4ea43a4`, `07decad` |
| 4 — Color animation (fill/stroke) | `specs/…slice4-color-animation-design.md` | `6e1084a`, `23df609` |
| 5 — Motion paths | `specs/…slice5-motion-paths-design.md` | `514661d` |
| 6 — Primitives (polygon/star/line) | `specs/…slice6-primitives-design.md` | `186ea2f` |
| 7 — Freehand brush | `specs/…slice7-freehand-brush-design.md` | `dc7d920` |
| 8 — Gradients (linear/radial, static) | `specs/…slice8-gradients-design.md` | `9232596` |
| 9 — Animated gradients | `specs/…slice9-animated-gradients-design.md` | `3c8f9df` |
| 10 — Stroke dash & self-drawing | `specs/…slice10-stroke-dash-design.md` | `8dbd0af` |
| 11 — On-canvas gradient handles | `specs/…slice11-gradient-handles-design.md` | `091c419` |
| 12 — SVG rotate handle | `specs/…slice12-rotation-handle-design.md` | (see spec) |
| 13 — Onion skinning | `specs/…slice13-onion-skinning-design.md` | (see spec) |
| 14 — Duplicate object | `specs/…slice14-duplicate-object-design.md` | (see spec) |
| 15 — Delete object | `specs/…slice15-delete-object-design.md` | (see spec) |
| 16 — Reorder objects | `specs/…slice16-reorder-objects-design.md` | (see spec) |
| 17 — Layers panel | `specs/…slice17-layers-panel-design.md` | (see spec) |
| 18 — Rename object | `specs/…slice18-rename-object-design.md` | (see spec) |
| 19 — Object lock | `specs/…slice19-object-lock-design.md` | (see spec) |
| 20 — Layers drag-reorder | `specs/…slice20-layers-drag-reorder-design.md` | (see spec) |
| 21 — Object clipboard (copy/cut/paste) | `specs/…slice21-clipboard-design.md` | (see spec) |
| 22 — SVG rotate handle (cont.) | `specs/…slice22-svg-rotate-handle-design.md` | (see spec) |
| 23 — Scale handles | `specs/…slice23-scale-handles-design.md` | (see spec) |
| 24 — Copy/paste keyframes | `specs/…slice24-copy-paste-keyframes-design.md` | (see spec) |
| 25 — Drag-to-retime keyframes | `specs/…slice25-drag-keyframe-retime-design.md` | `98797b0` |
| 26 — Edge scale handles | `specs/…slice26-edge-scale-handles-design.md` | `833ed64` |
| 27 — Lock-aware timeline | `specs/…slice27-lock-aware-timeline-design.md` | (see spec) |
| 28 — Uniform (shift) scale/resize | `specs/…slice28-uniform-scale-resize-design.md` | `d17c4a0` |
| 29 — Cut keyframe | `specs/…slice29-cut-keyframe-design.md` | `1c7cde5` |
| 30 — Alt scale/resize from centre | `specs/…slice30-scale-from-center-design.md` | `26f0d94` |
| **True-M2-polish program (31–35):** | | |
| 31 — Curve-tight `pathBounds` | `specs/…slice31-curve-tight-bounds-design.md` | `17d6073` |
| 32 — Rounded polygon/star corners | `specs/…slice32-rounded-primitive-corners-design.md` | `295c979` |
| 33 — Stage snapping / alignment guides | `specs/…slice33-snapping-guides-design.md` | `b0985a6` |
| 34 — Gradient stop-count morphing | `specs/…slice34-gradient-stop-morph-design.md` | `2df60b4` |
| 35 — Parametric primitive re-editing | `specs/…slice35-parametric-primitives-design.md` | `7817bc2` |

## Milestone 4 — Grouping, layers & nested symbols (COMPLETE)

Layers panel, object lock, **visibility** (eye toggle), and drag-reorder shipped during M2
(slices 17/19/20). Multi-object selection/transform (36–44), grouping (45a–45f), and boolean
ops (46) are COMPLETE. **Nested symbols — the last M4 headline — is fully COMPLETE: foundation
(47a), instance transform UI (47b), symbol EDIT MODE (47-edit), independent per-instance TIMELINES
(47c), and the symbols LIBRARY (47d).** Remaining M4: "author inside a symbol" (route the deferred
in-symbol draw/delete/group actions to the active scene) + bounded grouping/boolean polish.

| Slice / feature | Spec | Merge |
|-----------------|------|-------|
| 36 — Multi-select foundation (Shift/Cmd-click; bulk delete/duplicate; multi-highlight; Inspector multi-state) | `specs/2026-06-22-savig-m4-slice36-multi-select-design.md` | `20c5135` |
| 37 — Multi-object move (drag a member → all move; arrows nudge all; outlines follow) | `specs/2026-06-22-savig-m4-slice37-multi-move-design.md` | `466ebb0` |
| 38 — Marquee (rubber-band) selection (drag the empty background → select intersecting; Shift adds) | `specs/2026-06-22-savig-m4-slice38-marquee-design.md` | `5241108` |
| 39 — Multi-object copy/cut/paste (clipboard → list; bulk; drops the cut collapse) | `specs/2026-06-22-savig-m4-slice39-multi-clipboard-design.md` | `3bb3763` |
| 40 — Multi-object scale (group bbox + 8 handles; scale all about the opposite corner/edge) | `specs/2026-06-22-savig-m4-slice40-multi-scale-design.md` | `21d95f5` |
| 41 — Multi-object rotate (group rotate handle; rotate all about the group centre) | `specs/2026-06-22-savig-m4-slice41-group-rotate-design.md` | `8427b61` |
| 42 — Grouping, phase 1 (selection-grouping via `groupId`; Group/Ungroup; click/marquee selects the group; Cmd+G / Cmd+Shift+G; Inspector buttons) | `specs/2026-06-22-savig-m4-slice42-grouping-design.md` | `8334a38` |
| 43 — Align & distribute (6 align + 2 equal-gap distribute; pure AABB geometry → `setObjectsTransforms`; movable-count gating; lifted `objectAABB`/`resolveObjectAnchor` into `snapping.ts`) | `specs/2026-06-22-savig-m4-slice43-align-distribute-design.md` | `5a175f4` |
| 44 — Multi-object move snapping (drag a multi-selection → the group bbox snaps to other objects + artboard, reusing `computeSnap`/`groupBBox`; respects `snapEnabled`; guides) | `specs/2026-06-22-savig-m4-slice44-multi-snap-design.md` | `62a98c7` |
| **45a — Group containers: compose engine (FOUNDATION)** (group object `isGroup`+`parentId`; transform composes onto children by PREPENDING a string in the shared `computeFrame`+`renderDocument` — no DOM nesting; `groupTransformPrefix`/`bakeGroupIntoChild`/`createGroupObject`; non-breaking + dormant; parity preserved) | `specs/2026-06-22-savig-m4-slice45-group-container-design.md` | `676a2a7` |
| **45b — Group containers: store + UI** (groupSelected creates a container + selects it; ungroupSelected bakes into children; setGroupTransform writes the static base; member-click selects the GROUP; move + reused slice-40/41 bbox handles scale/rotate it w/ live preview composed onto children; `groupId` removed; Inspector group panel; delete cascades; 3-pass review loop) | ↑ (same spec) | `24aa137` |
| **45c — Layers tree** (group rows + disclosure toggle + children nested/indented + expand/collapse; pure `isRenderHidden` group-visibility cascade applied at Stage `ordered` / `renderDocument` body+defs / marquee; row-click selects the group; drag-reorder top-level only; 3-pass review loop) | `specs/2026-06-22-savig-m4-slice45c-layers-tree-design.md` | `c9f6f8d` |
| **45d — Animatable group transform** (a group keyframes when auto-key is on, base when off — `applyObjectTransform` gains an `autoKey` arg, its only production change; the shared `computeFrame`/`groupTransformPrefix(time)` already animate children in preview+export; Timeline+duration already count group tracks; ungroup of an animated group bakes t=0, drops animation — v1) | `specs/2026-06-22-savig-m4-slice45d-animatable-group-design.md` | `f0bfe04` |
| **45e — Nested groups** (group-in-group; the shared one-level helpers walk the parent CHAIN — `groupTransformPrefix` composes ancestors outermost-first, `isRenderHidden`/`resolveToEntity` walk up, `groupAABB`/Layers/delete-cascade recurse, all cycle-guarded; `ungroupSelected` reparents to the first surviving ancestor; 4-pass review loop) | `specs/2026-06-22-savig-m4-slice45e-nested-groups-design.md` | `cdbd00a` |
| **45f — Drag-reparent** (drag a Layers row onto a group to add / onto a top-level row to remove, preserving world position; reparent = bake-OUT the old ancestor chain + unbake-IN the new chain via the new exact-inverse `unbakeGroupFromChild`/`invMapPoint`; cycle + same-parent guards; reviewed clean) | `specs/2026-06-22-savig-m4-slice45f-drag-reparent-design.md` | `49a1b83` |
| **46 — Boolean path ops** (Union/Subtract/Intersect/Exclude on ≥2 vector shapes → ONE new path object; engine `geom/boolean.ts` world-bakes each flattened outline through its object+group transform chain — exported `mapPoint` — and clips via the new `polygon-clipping` dep; subtract = upper from the bottom-most by zOrder; holed/disjoint results stored as `VectorAsset.compoundRings`, rendered `fill-rule:evenodd` via the shared `pathToDRings` in BOTH the editor Stage and the export runtime (`pathBoundsRings`/`shapeLocalBBox` span the rings); destructive replace — undoable, prunes orphaned source assets; Inspector buttons gate on ≥2 non-group vector operands; v1 limits: curves→polygons, animated sources collapse to a snapshot, compound rings not node-editable, groups/SVG-asset objects not operands; 2-pass review loop — Important asset-prune fixed, then clean) | `specs/2026-06-22-savig-m4-slice46-boolean-ops-design.md` | `b2580ab` |
| **47a — Nested symbols: FOUNDATION** (Flash-style reusable animated clips — the LAST M4 headline, decomposed into 47a–47d. A symbol DEFINITION is a new `SymbolAsset` Asset kind carrying its own self-contained `objects[]` scene; a symbol INSTANCE introduces NO new object field — it's an ordinary `SceneObject` whose `assetId` points at a SymbolAsset, exactly parallel to an svg-asset object → `<use>`. So instancing/duplicate/edit-propagation come FREE off the shared `assetId`. The hard part — a single recursive scene-walker `engine/symbol.ts::flattenInstances(project,time)` — is THE source of truth for all three render consumers: it sorts each scene by (zOrder,index), skips group containers (folding their `groupTransformPrefix` into descendants), expands symbol instances composing transform+opacity and namespacing ids `instId/childId`, cycle-guarded by a per-path visited-asset Set, and emits drawable `InstanceLeaf`s (renderId/object/transformPrefix/opacityFactor/**localTime** — the 47c time-remap seam, == global time in 47a). `computeFrame`, `renderSvgDocument` (export FLATTENS per-leaf rather than `<use>` — forward-compat with 47c's independent timelines), and the editor Stage skeleton all consume leaves → **preview==export parity by construction** (verified line-by-line vs base + parity tests). `groupTransformPrefix`/`parentGroupOf` refactored to take a scene `objects[]`. `createSymbol` store action + Inspector button (≥1 selection, single-object panel too) move the selection into a new SymbolAsset + one identity-wrapper instance (parity-preserving, undoable). v1 LIMITS (deferred): instance is ATOMIC — selection-highlight + move only, NO bbox handles/outline/snapping/live-preview (47b), no enter/edit-internals (47b), internals sample at GLOBAL time so all instances show the same frame (47c), no library panel (47d). 1-pass review CLEAN — 0 Critical/0 Important, 2 Minor fixed [single-object button reach + group-descendant-pull test]; 1027 unit + 57 e2e + typecheck + lint green) | `specs/2026-06-23-savig-m4-slice47-nested-symbols-design.md` · `plans/2026-06-23-savig-m4-slice47a-nested-symbols-foundation.md` | `4701790` |
| **47b — Nested symbols: INSTANCE TRANSFORM UI** (the §6-deferred half of the slice47-spec "47b" — the OTHER half, symbol EDIT MODE, is intentionally split into its own next slice). KEY INSIGHT: a symbol instance is structurally a NODE-LESS CONTAINER, exactly like a group (47a made it an ordinary `SceneObject`→`SymbolAsset` rendering as flattened composite-id leaves with no DOM node), so 47b is mostly GENERALIZING the group transform chrome to fire for instances. New editor-only geometry in `Stage/snapping.ts` (NO engine/runtime/export change → parity untouched): `isSymbolInstance`, `instanceAABB` (the symbol scene's content box mapped through the instance transform M — mirrors `groupAABB`'s corner-map), `sceneContentAABB` (union of a scene's top-level entity boxes), `entityAABB` (dispatch group/instance/plain); `groupAABB` extended so an instance CHILD contributes its box. Stage wiring: `groupBounds`→`instanceAABB` so a single instance shows the reused slice-40/41 bbox + 8 scale handles + rotate handle; `previewInstanceChildren` = SURGICAL live preview (recompute `computeFrame` on a project where the instance carries the in-progress transform as a tracks-stripped base, then `applyFrameToNodes` ONLY this instance's own `instId/…` leaves — no full repaint, no sibling-preview clobber); move-snapping via `entityAABB` baseAABB + instances as snap targets. DESIGN CORRECTION (vs the spec's loose wording): an instance is autoKey-GATED like ANY object (spec §2: instance transforms animate like any object) — NOT a base-writing container like a group — so the commit flows through the existing `setObjectsTransforms`/`setProperties` object paths (keyframes when auto-key on); this made the slice smaller (no store changes, no `isSingleContainerSelected`). CYCLE-SAFE: `instanceAABB`/`sceneContentAABB`/`groupAABB` thread a visited-ASSET set (independent of the parentId object-`seen` set), copy-before-mutate, terminating group-mediated symbol cycles (A→group→instance-of-A). 2-PASS review loop (feature-dev:code-reviewer): pass 1 = 0 Crit / 2 Important (group-mediated cycle unguarded in AABB traversal → fixed by threading `seenAssets`; full-repaint preview reverted sibling previews in a mixed multi-select drag — reviewer's "instances last" fix was BACKWARDS, fixed instead by the surgical per-leaf preview); pass 2 = CLEAN (0/0, both fixes verified, prefix-filter collision-safe via trailing `/`). 1042 unit + 58 e2e + typecheck + `src`/`e2e` lint green. v1 cosmetic-only gaps (commit always correct, deferred): instance-inside-a-GROUP drag-preview lag; multi-select `groupBounds` doesn't span an instance's bbox; multi-select MOVE doesn't live-preview instance leaves | `specs/2026-06-23-savig-m4-slice47-nested-symbols-design.md` · `plans/2026-06-25-savig-m4-slice47b-instance-transform-ui.md` | `a42718a` |
| **47-edit — Nested symbols: SYMBOL EDIT MODE** (edit-in-place: double-click an instance to descend into its internal scene; the OTHER half of the slice47-spec "47b", split from the shipped transform UI). PIVOTAL INSIGHT: a symbol is a GLOBAL asset, so "the scene being edited" is ONE flat array — root `project.objects` or one `SymbolAsset.objects` — regardless of nav depth; thus NO new data/persistence/undo plumbing (history already snapshots the whole Project incl. assets), only read/write REDIRECTION. Transient `editPath: string[]` (symbol-asset ids entered) drives a "focused scene": `selectActiveObjects` (active scene's objects; stable ref; missing-asset→root fallback) + `selectEditProject` (focused project, same `present` ref at root) feed Stage/Timeline/LayersPanel/Inspector + the scene selectors; `commitActiveScene` writes back to the asset (or root) then snapshots the whole project. v1 routes ONLY the transform actions (`setProperties`/`nudgeSelected`/`setObjectsTransforms`)→active scene; NO-OP-SAFETY: every other selection-dependent action safely early-returns in edit mode (the internal id is absent from root.objects — pass-2 review AUDITED delete/dup/group/boolean/reorder/reparent/keyframes/anchor/style/gradient = all safe no-ops), and create tools are gated by `setActiveTool` forcing `select` while in edit mode. Enter = double-click instance leaf→`enterSymbol`; exit = `exitSymbol`/`exitToDepth`/Esc + the new `EditBreadcrumb` (Root › Sym › …); enter/exit clear selection. DESIGN: an instance animates like ANY object (spec §2), so edit mode is autoKey-gated, NOT a container — keeps v1 small. Edit-PROPAGATION is free (all instances read the same asset). NO engine/runtime/export change → parity untouched; `editPath` never serialized (export renders root). 2-PASS review: pass 1 = 0 Crit / 1 Important (`clearStaleSelection` in undo/redo filtered against ROOT objects → wiped a valid internal selection on every undo in a symbol; fixed to scope to the active scene); pass 2 = CLEAN (fix verified + the no-op-safety audit above). 1060 unit + 59 e2e + typecheck + `src`/`e2e` lint green. v1 DEFERRED → "author inside a symbol": in-symbol delete (needs cross-scene asset-prune), draw/pen/primitive/brush, node-edit, group/boolean/clipboard, Layers mutators (eye/lock/rename/reorder); nested-edit beyond breadcrumb cosmetics | `specs/2026-06-25-savig-m4-slice47-symbol-edit-mode-design.md` · `plans/2026-06-25-savig-m4-slice47-symbol-edit-mode.md` | `3663a25` |
| **47c — Nested symbols: INDEPENDENT PER-INSTANCE TIMELINES** (each instance runs its own internal clock → two instances of one symbol show DIFFERENT frames at the same wall-clock moment, the real Flash-MovieClip payoff). PIVOTAL: the engine seam was ALREADY there — `flattenInstances` threads a `localTime` down its recursion and samples every leaf at it (in 47a–edit it was just the global time); 47c replaces the ONE identity line with a per-instance REMAP. New optional `SceneObject.symbolTime?: { startOffset; loop; speed }` — **ABSENT = identity remap**, the parity-safety mechanism (existing projects + the parity test are byte-unchanged; a fresh instance stays in lockstep until the user opts in). `remapLocalTime(parentTime,timing,symbolDuration)` = shift by startOffset, scale by speed, hold-first-frame before start (`t<=0→0`), then LOOP (`t%dur`, t>0 guaranteed so no negative-mod) or ONE-SHOT (`min(t,dur)`); `symbolDuration<=0→0` (no NaN). Symbol intrinsic duration = `objectsMaxKeyframeTime(asset.objects)` (extracted from `computeProjectDuration` as a shared helper; computed per-frame). Seam: the INSTANCE's own transform still samples at the parent `localTime`; only its INTERNALS recurse at `childTime` (= remap when `symbolTime` present, else `localTime`); NESTING composes by recursion (B's parentTime = A's childTime). EXPORT is FREE — the runtime ticks `computeFrame→flattenInstances` per frame, so preview==export by construction (no `renderDocument`/runtime change; new parity test with a timed instance). `setSymbolTiming(partial)` merges + clamps (`startOffset≥0`, `speed≥1e-3`), active-scene routed (works inside a symbol in edit mode), undoable; Inspector "Symbol timing" panel (start offset / loop / speed) for a selected instance. 1-PASS review CLEAN (0 Crit/0 Imp, 2 Minor fixed: stale `SymbolAsset.duration` comment + a nested-composition test — writing the test caught a wrong expectation: a symbol whose only content is a keyframe-less nested instance has 0 intrinsic duration → its remap collapses to 0, the documented v1 0-duration edge, NOT a bug). 1080 unit + 60 e2e + typecheck + `src`/`e2e` lint green. v1 DEFERRED: keyframing the timing fields, `SymbolAsset.duration` manual override (currently UNREAD — reserved), ping-pong/play-N/random-start, per-instance loop length on the Timeline ruler | `specs/2026-06-25-savig-m4-slice47c-independent-timelines-design.md` · `plans/2026-06-25-savig-m4-slice47c-independent-timelines.md` | `bc15684` |
| **47d — Nested symbols: SYMBOLS LIBRARY** (the last nested-symbols sub-slice — browse/place/swap symbols, completing the Flash-style feature). Mostly ASSEMBLING existing primitives: symbols are already `Asset`s (so they already showed in the AssetPanel — with a BROKEN click that fell through to `addAudioClip`, now fixed); 47-edit's `commitActiveScene` already lets place/swap target the root OR a symbol in edit mode; 47b's `sceneContentAABB` gives the placed instance's content-centre anchor; `flattenInstances`'s visited-set is the render-time cycle backstop. Genuinely NEW = two pure engine helpers + two store actions + UI. **`symbolContains(containerSymId, targetSymId, assets)`** = the authoring-time **CYCLE GUARD #2** (transitive reachability, visited-Set guarded so a corrupt graph terminates); **`countSymbolInstances(symId, scene)`** = usage count across root + every symbol's objects (param narrowed to `Pick<Project,'objects'|'assets'>` so the UI subscribes narrowly). **`placeSymbolInstance(symId)`** appends an instance to the ACTIVE scene (content-centre anchor; selects it), **`swapSymbol(instanceId, newSymId)`** repoints only `assetId` (preserves transform/`symbolTime`); both CYCLE-GUARDED (active scene `C`: reject if `symId===C \|\| symbolContains(symId,C,assets)`; at root `C`=null→always allowed) with an error toast, undoable. CYCLE-GUARD COMPLETENESS (reviewer-confirmed): checking ONLY the deepest `activeAssetId` is sufficient — a new edge `C→Y` creates a cycle iff Y already reaches C, and since `editPath` is built by `enterSymbol` (each level's symbol directly contains the next), any ancestor cycle is caught transitively by `symbolContains(Y, deepest)`. UI: AssetPanel "Symbols" section (name + live count + click-to-place; cyclic rows disabled in edit mode; symbols filtered out of the generic list) + Inspector "Swap symbol" select (cycle-filtered targets). NO engine-render change → parity untouched (pure read helpers). 1-PASS review CLEAN (0 Crit/0 Imp, 1 Minor fixed: AssetPanel subscribed to whole `history.present`→narrowed to objects+assets). 1093 unit + 61 e2e + typecheck + `src`/`e2e` lint green. v1 DEFERRED: rendered thumbnails (need a content render-snapshot), drag-to-place + drop-point (v1 click-to-place), recompute-anchor-on-swap, rename/delete-symbol management | `specs/2026-06-26-savig-m4-slice47d-symbols-library-design.md` · `plans/2026-06-26-savig-m4-slice47d-symbols-library.md` | `147bc4d` |
| **Author-in-symbol PHASE 1 — IN-SYMBOL DELETE + cross-scene asset prune** (the first 47-edit follow-up: 47-edit routed only TRANSFORM actions to the active scene; the rest no-op'd. This routes DELETE.) `deleteSelectedObject` was a silent no-op inside a symbol (read root `project.objects` where an internal id is absent). Rewritten to be ACTIVE-SCENE-aware (`selectActiveObjects`/`selectActiveAssetId`): gather selected non-locked ids in the active scene, cascade group descendants WITHIN it, write the active scene back (root objects OR the edited symbol asset), then PRUNE. Doing it correctly forced a LATENT-BUG FIX: the old `removeObject` pruned a deleted object's asset by ROOT usage only, so a vector/svg asset shared between root and a symbol was WRONGLY pruned (breaking the symbol) when its root user was deleted. New pure engine helper **`collectReferencedAssetIds(project)`** (every assetId across root + all symbol scenes) backs a CROSS-SCENE, SYMBOL-PRESERVING prune: a deleted object's asset is a candidate; KEEP if `symbol`/`audio` (library defs/audio never pruned by an object delete) OR still referenced in the POST-delete project; prune only an unreferenced `vector`/`svg`. THREE corrections (all reviewer-traced, no test asserted the old behavior): in-symbol delete works; cross-scene shared asset kept; **library symbol persists at 0 instances** (Flash-style). NO UI change (Delete key + Inspector button already call it). NO engine-render change → parity intact (pure read helper). `removeObject` now unused by the store (import swapped) but kept as a tested engine util. v1 note: `cut` inside a symbol now DELETES but the COPY half still no-ops (clipboard routing deferred). 1-PASS review CLEAN (0 Crit/0 Imp/0 Minor; reviewer traced the prune predicate + 5 edge cases). 1098 unit + 62 e2e + typecheck + `src`/`e2e` lint green | `specs/2026-06-26-savig-m4-author-in-symbol-delete-design.md` · `plans/2026-06-26-savig-m4-author-in-symbol-delete.md` | `8a75aa7` |
| **Author-in-symbol PHASE 2 — IN-SYMBOL DRAW** (route the geometry-CREATE actions to the active scene + un-gate the draw tools in edit mode). The 3 create actions (`addVectorShape` rect/ellipse, `addPrimitive` polygon/star, `addVectorPath` line/pen/brush) shared an identical commit tail; factored into one pure helper **`appendObjectToScene(project, activeAssetId, asset, obj)`** — the ASSET/OBJECT SPLIT: the new asset always goes GLOBAL (`project.assets`; a symbol's internals reference global assets, as `flattenInstances` expects), the new OBJECT goes to the ACTIVE scene (root `project.objects` OR the edited symbol's `objects[]`). Each action reads `selectActiveObjects`/`selectActiveAssetId`, computes zOrder over the ACTIVE scene, commits via the helper; after-draw tool = `select` inside a symbol (node-edit-in-symbol not routed yet → don't drop the user on a dead-end tool) vs the existing `node` for paths at root. 47-edit's `setActiveTool` gate relaxed: `SYMBOL_EDIT_TOOLS = {select,rect,ellipse,polygon,star,line,pen,brush}` allowed in edit mode; `node`/`motion` still GATED (their edit actions aren't routed). Edit-PROPAGATION free (new object in `SymbolAsset.objects` → every instance renders it via flattenInstances). NO engine-render change → parity intact; root draw byte-unchanged (`activeId` null → helper returns today's project). 1-PASS review CLEAN (0 Crit/0 Imp/0 Minor; reviewer verified all 7 checks + parity). 1102 unit + 63 e2e + typecheck + `src`/`e2e` lint green. DEFERRED to later phases: node-edit, motion, group/boolean, clipboard, Layers-mutators (eye/lock/rename/reorder — `renameObject` etc. still route to root, no-op in a symbol) inside | `specs/2026-06-26-savig-m4-author-in-symbol-draw-design.md` · `plans/2026-06-26-savig-m4-author-in-symbol-draw.md` | `537bf24` |
| **Author-in-symbol PHASE 3 — IN-SYMBOL NODE-EDIT** (route the path node-edit writes to the active scene + un-gate the `node` tool). TWO SEAMS made it small: (1) ALL node geometry editing (drag via `usePathTools`, insert, delete, smooth, join) funnels through `setPathData`; (2) the object is resolved in ONE place — `selectedPathCtx` (shared by `setPathData`/`addShapeKeyframe`/`removeShapeKeyframe`) — and a STATIC path edit writes the GLOBAL asset (`asset.path`), so it works inside a symbol the moment the object is found. So: scope `selectedPathCtx` to `selectActiveObjects` (object from the active scene; asset stays global), add pure **`replaceObjectInScene(project, activeAssetId, next)`** (root → `replaceObject`; else replace inside the edited symbol asset's `objects[]`) — the GENERALIZATION of 47-edit's `commitActiveScene` to the "replace one object" case that most editors use — and route the 3 MORPH-branch object writes (`setPathData` shapeTrack branch, `addShapeKeyframe`, `removeShapeKeyframe` both branches) through it; the static/asset writes need no change (global). Add `'node'` to `SYMBOL_EDIT_TOOLS`; revert phase-2's after-draw tool so in-symbol path draws land on `node` again. Edit-PROPAGATION free (node/morph edit → symbol asset → all instances). NO engine-render change → parity intact; root node-edit byte-unchanged (`replaceObjectInScene(p, null, x)` === `replaceObject`). DEFERRED & SAFE: advanced morph fine-tuning (`setSelectedNodeEasing`/`setSelectedShapeKeyframeMorph`/`Correspondence`) still resolves obj from ROOT → `!obj?.shapeTrack` guard early-returns in a symbol (no corruption, reviewer-traced); in-symbol PAINT (color/gradient/style/dash use `replaceObject` → reuse `replaceObjectInScene` next). 1-PASS review CLEAN (0 Crit/0 Imp, 1 Minor fixed: stale gate comment; reviewer traced all 7 checks + the 2-write compose). 1106 unit + 64 e2e + typecheck + `src`/`e2e` lint green | `specs/2026-06-26-savig-m4-author-in-symbol-nodeedit-design.md` · `plans/2026-06-26-savig-m4-author-in-symbol-nodeedit.md` | `c10dff4` |
| **Author-in-symbol PHASE 4 — IN-SYMBOL PAINT (+ anchor)** (route the appearance + anchor actions to the active scene). The CHEAPEST phase — pure application of the phase-3 seam: each of 6 actions resolves the object via `selectActiveObjects` (not root) and routes its OBJECT writes through `replaceObjectInScene(project, selectActiveAssetId(s), …)`; its ASSET writes (`assets.map`, static `VectorStyle`) stay global. `setVectorStyle` (object resolve only), `setVectorColor` (autoKey→colorTracks via seam; !autoKey→setVectorStyle), `setStrokeDashoffset` (autoKey→dashOffsetTrack), `setAnchor` (anchorX/Y); `setVectorGradient` + `setStrokeDasharray` have DUAL-WRITE "clear" branches (clear the asset style AND the object's animated track in one commit) routed by building `withAssets` then `replaceObjectInScene(withAssets, …)` — exactly phase-3's compose; the vector asset and the symbol asset are distinct ids, no collision. autoKey/STATIC SPLIT: static paint writes the GLOBAL asset (works once the object is found); only animated tracks + anchor need scene-aware object writes. Edit-PROPAGATION free; NO engine-render change → parity intact; root paint byte-unchanged (`replaceObjectInScene(p,null,x)`===`replaceObject`). 1-PASS review CLEAN (0 Crit/0 Imp/0 Minor; reviewer traced all 7 checks incl. dual-write compose + no-mutation). 1114 unit + 65 e2e + typecheck + `src`/`e2e` lint green. DEFERRED: Layers-mutators (visibility/lock/rename/reorder), clipboard, group/boolean, motion, advanced morph fine-tuning inside | `specs/2026-06-26-savig-m4-author-in-symbol-paint-design.md` · `plans/2026-06-26-savig-m4-author-in-symbol-paint.md` | `79a0ff6` |
| **Author-in-symbol PHASE 5 — IN-SYMBOL LAYERS MUTATORS** (route the six Layers-panel mutator store actions to the active scene; the Layers panel already SHOWED the active scene via 47-edit but its row buttons resolved/wrote the ROOT). Pure application of the established seam: each action scopes its read to `selectActiveObjects(s)` and writes via `replaceObjectInScene(project, selectActiveAssetId(s), …)` (single-object) or `commitActiveScene(objects)` (whole-array). `toggleObjectVisibility`/`toggleObjectLock`/`renameObject` = single-object field writes (the lock selection-drop still operates on scene-local `selectedObjectIds`, unchanged); `reorderSelected`/`moveObjectToTarget` = array writes (no-op short-circuit compares against the active-scene array); `reparentObject` = re-point its single local `const objs = project.objects` → `selectActiveObjects(s)` so the WHOLE world-position bake math (cycle guard + parentGroup walk + bake-out/unbake-in, slice-45f) reads the active scene, final write via `replaceObjectInScene` (the bake helpers read GLOBAL `project.assets` — intentional, unchanged). Edit-PROPAGATION free (mutating a symbol's `objects[]` → every instance via flattenInstances; a hidden internal part → `isRenderHidden` → dropped leaf in every instance + export). NO engine-render change → parity intact; root behavior byte-unchanged (activeAssetId null ⇒ `replaceObjectInScene`≡`replaceObject`, `commitActiveScene`≡`commit{…objects}`). 2-PASS review: pass 1 = 0 Crit / 1 Important (missing `moveObjectToTarget` in-symbol unit test → added; reviewer confirmed all six routings correct, reparent read/write same-scene, parity untouched), pass 2 = CLEAN (0/0; fix verified, code clean). 1120 unit + 66 e2e + typecheck + `src`/`e2e` lint green. DEFERRED: clipboard (copy/paste), group/boolean, motion, advanced morph fine-tuning inside | `specs/2026-06-26-savig-m4-author-in-symbol-layers-design.md` · `plans/2026-06-26-savig-m4-author-in-symbol-layers.md` | `69d7d56` |
| **Author-in-symbol PHASE 6 — IN-SYMBOL CLIPBOARD (copy/paste)** (route the OBJECT clipboard to the active scene + a paste-time cycle guard). Inside a symbol `cut` already DELETED (phase 1) but it calls `copySelected` FIRST, and `copySelected` read the ROOT → the COPY half no-op'd (cut deleted without snapshotting); `paste` APPENDED to the root, so pasting "inside" a symbol silently dumped objects into the root scene. Fix: `copySelected` reads `selectActiveObjects(s)` (asset lookup stays GLOBAL — symbol internals reference global assets); `paste` routes its object append + incremental zOrder to the active scene via two NEW pure helpers — `sceneObjectsOf(project, activeAssetId)` (read the active scene's objects[] from any project — the READ dual) + `appendToScene(project, activeAssetId, obj)` (append ONE object, no asset add — the object-only WRITE), with phase-2's `appendObjectToScene` refactored to COMPOSE `appendToScene` (DRY, byte-identical); the clonedAsset/cross-project re-add stays GLOBAL. `cut` needed NO code change (it's `copySelected`+`deleteSelectedObject`, both now scene-aware → cut works fully inside a symbol). PASTE-TIME CYCLE GUARD: pasting a symbol INSTANCE into a symbol it would (transitively) contain authors a cycle — skip the entry (`isSymbolInstance && (assetId===activeAssetId || symbolContains(assetId, activeAssetId, assets))`, the SAME predicate placeSymbolInstance/swapSymbol use, 47d guard #2) + one error toast; a `pasted` flag (NOT `selectIds.length`) gates the commit so a non-cyclic LOCKED clone still commits while an all-cyclic paste early-returns without an empty undo step. Cross-scene paste (copy in a symbol, paste at root / another symbol) falls out FREE — the clipboard holds scene-agnostic object+asset snapshots. NO UI/keyboard change (useKeyboard already calls copy/cut/paste, no edit-mode gate); NO engine-render change → parity intact; root copy/paste byte-unchanged (`sceneObjectsOf(p,null)`=`p.objects`, `appendToScene(p,null,o)`=`{…p,objects:[…,o]}`). 2-PASS review: pass 1 = 0 Crit / 1 Important (transitive `symbolContains` cycle-guard branch had NO test — only the `===` branch; added a B-contains-A transitive test → a future arg-swap regression now caught), pass 2 = CLEAN (0/0; transitive test verified to genuinely exercise `symbolContains`). 1126 unit + 67 e2e + typecheck + `src`/`e2e` lint green. DEFERRED: group/boolean, motion, advanced morph fine-tuning inside | `specs/2026-06-26-savig-m4-author-in-symbol-clipboard-design.md` · `plans/2026-06-26-savig-m4-author-in-symbol-clipboard.md` | `d01dbbc` |
| **Author-in-symbol PHASE 7 — IN-SYMBOL GROUP / BOOLEAN** (route `groupSelected`/`ungroupSelected`/`booleanOp` to the active scene). group/ungroup are pure `objects[]` restructures → scope every scene read to `selectActiveObjects(s)` (incl. the `groupAABB` child-walk + the `objects.map`/`.filter` bake/reparent; assets stay GLOBAL — a group has `assetId:''`, `bakeGroupIntoChild` takes the group+child directly) and write the array via `commitActiveScene`. `booleanOp` (the meaty one): eligible read → active scene; pass a SCENE-SCOPED project `{ ...project, objects: activeObjects }` to `booleanOpEngine` so its `parentGroupOf(project.objects,…)` world-bake resolves intra-symbol groups (NO `engine/geom/boolean.ts` change; at root `activeObjects===project.objects` → byte-identical); zOrder/removal over the active scene; the new `VectorAsset.compoundRings` goes GLOBAL, the result object to the active scene via the NEW pure **`withSceneObjects(project, activeAssetId, objects)`** (the array-write dual of `sceneObjectsOf`; `commitActiveScene` refactored to COMPOSE it, byte-identical). LATENT-BUG FIX (same class as phase-1 delete): the old boolean asset prune dropped a source vector asset when unreferenced in the ACTIVE scene only → a vector asset shared with ANOTHER scene was wrongly pruned; now the prune uses phase-1's cross-scene `collectReferencedAssetIds(postOpProject)` predicate (candidate=source assetIds; keep if not-a-candidate OR symbol/audio OR still referenced anywhere). Edit-PROPAGATION free; NO engine-render change → parity intact; NO UI/keyboard change (Inspector multi-select panel already reads `selectActiveObjects`; Cmd+G/Cmd+Shift+G already fire in edit mode). 1-PASS review CLEAN (0 Crit/0 Imp; reviewer traced commitActiveScene byte-identity, the scoped-engine bake, and both cross-scene prune branches; its one sub-threshold note — "no in-symbol boolean e2e" — was the reviewer reading the unchanged `boolean-ops.spec.ts`; the e2e IS in `symbols.spec.ts` and passes). 1130 unit + 68 e2e + typecheck + `src`/`e2e` lint green. DEFERRED: motion paths, advanced morph fine-tuning inside | `specs/2026-06-26-savig-m4-author-in-symbol-group-boolean-design.md` · `plans/2026-06-26-savig-m4-author-in-symbol-group-boolean.md` | `4dc605c` |
| **Author-in-symbol PHASE 8 — IN-SYMBOL MOTION PATHS** (un-gate the `motion` tool inside a symbol + route the motion-path actions to the active scene). Added `'motion'` to `SYMBOL_EDIT_TOOLS` (it now covers ALL 10 ToolModes — the edit-mode tool gate is an effective no-op but the explicit set stays as the documented allow-list/extension point). Routed the 5 motion-path actions — `addMotionPath`/`removeMotionPath`/`setMotionPathOrient`/`setMotionProgress`/`removeSelectedProgressKeyframe` — via the phase-3/4 single-object seam (resolve `selectActiveObjects(s)`, write `replaceObjectInScene(project, selectActiveAssetId(s), …)`). EVERYTHING ELSE ALREADY active-scene-aware (NO change): the Stage motion-guide overlay reads the edit-scoped `project` (`{ ...present, objects: activeObjects }`); the motion tool draw (`usePathTools` → `addMotionPath(selectedObjectId, …)`); the Inspector motion controls + Timeline progress track (47-edit scoped); and `sample.ts` applies `motionPath` + `duration.ts` counts its progress kfs → an internal object with a motion path animates in EVERY instance for free. NO engine-render change → parity intact; root motion byte-unchanged. 2-PASS review: pass 1 = 0 Crit / 1 Important + 1 Secondary (the motion PROGRESS keyframe is newly creatable inside a symbol, but its timeline ops — `retimeSelectedKeyframe`/`copyKeyframe`/`pasteKeyframe` progress branches — still resolved ROOT → silent no-op; FIXED by routing ONLY the progress branch in those 3 shared keyframe fns to the active scene, incl. a `pasteKeyframe` `clip.kind==='progress'` short-circuit before its shared root lookup; the scalar/shape/color/gradient/dash branches stay root-resolved = the separate pre-existing "in-symbol timeline keyframe editing" deferral, NOT introduced here), pass 2 = CLEAN (0/0; all 3 fixes verified byte-identical at root + no non-progress regression + parity intact). 1139 unit + 69 e2e + typecheck + `src`/`e2e` lint green. DEFERRED: advanced morph fine-tuning (the LAST author-in-symbol phase); general in-symbol timeline keyframe editing (scalar/color/shape/gradient/dash retime/copy/paste) | `specs/2026-06-26-savig-m4-author-in-symbol-motion-design.md` · `plans/2026-06-26-savig-m4-author-in-symbol-motion.md` | `9bf6699` |
| **Author-in-symbol PHASE 9 (FINAL) — IN-SYMBOL ADVANCED MORPH fine-tuning** → **"author inside a symbol" (phases 1–9) COMPLETE.** Route the morph fine-tuning actions to the active scene (phase-3 morph-write seam: resolve `selectActiveObjects(s)`, write `replaceObjectInScene(project, selectActiveAssetId(s), { ...obj, shapeTrack })`). The 4 actions — `setSelectedShapeKeyframeMorph` (morph mode corresponded/resampled), `setSelectedShapeKeyframeCorrespondence` (node map), `setSelectedNodeEasing` (per-node easing), `setCorrespondenceLink` (drag-link one A→B) — each wrote `obj.shapeTrack` (morph metadata) via root `replaceObject`, guarded by `!obj?.shapeTrack` to a safe no-op in a symbol (phase 3); now routed. EVERYTHING ELSE already active-scene-aware (NO change): `selectEditedShapeKeyframe`/`selectSelectedObject` (selectors scoped), Inspector morph controls (read `selectActiveObjects`), Stage correspondence drag-link overlay (edit-scoped `project.objects`), Timeline; and `samplePath` reads `shapeTrack` (morph/correspondence/nodeEasings) so an internal path's morph tuning renders in EVERY instance for free. NO engine-render change → parity intact; root morph byte-unchanged. 2-PASS review: pass 1 = 0 Crit / 1 Important (`setSelectedKeyframeEasing`'s SHAPE-keyframe branch — the morph-track interpolation easing — also resolved ROOT → the Inspector EasingEditor *displayed* it via active-scene `selectSelectedObject` but invoking no-op'd silently; FIXED by routing ONLY that shape branch [scalar/color/gradient/dash branches stay root = the timeline-editing deferral, same boundary as phase 8]), pass 2 = CLEAN (0/0; fix byte-identical at root + no non-shape regression + parity; reviewer confirmed `moveKeyframe`'s shape branch is the separate deferral, not a miss). 1145 unit + 70 e2e + typecheck + `src`/`e2e` lint green. DEFERRED: general in-symbol timeline keyframe editing (scalar/color/shape/gradient/dash retime/copy/paste/easing) — its own slice | `specs/2026-06-26-savig-m4-author-in-symbol-morph-design.md` · `plans/2026-06-26-savig-m4-author-in-symbol-morph.md` | `b140478` |
| **IN-SYMBOL TIMELINE KEYFRAME EDITING** → completes the in-symbol editing surface (the author-in-symbol phases routed each track type's *property-set* + motion `progress` + morph `shape` branches; the SHARED keyframe-manipulation fns still resolved ROOT for the other track types → a scalar/color kf was visible on the in-symbol Timeline but couldn't be removed/retimed/copied/pasted/re-eased). Routed EVERY remaining root-resolved branch to the active scene via the uniform seam (`selectActiveObjects(s).find` + `replaceObjectInScene(project, selectActiveAssetId(s), …)`; byte-identical at root): the 4 per-type removes (`removeSelectedKeyframe` scalar / `…ColorKeyframe` / `…GradientKeyframe` / `…DashKeyframe`), `setSelectedKeyframeRotationMode`, `setSelectedKeyframeEasing` (its progress/color/gradient/dash/scalar branches — incl. the progress-EASING missed in phase 8), `copyKeyframe` (5 non-progress lookups; clipboard-only, no commit; dropped a now-unused `p` binding), `retimeSelectedKeyframe` (5 non-progress branches), `pasteKeyframe` (shared lookup + hoisted `aid` + 5 switch-case writes; the phase-8 `progress` short-circuit untouched). All writes are OBJECT-field track writes (`tracks`/`colorTracks`/`gradientTracks`/`dashOffsetTrack`/`shapeTrack`) → NO engine-render change → parity intact; root keyframe ops byte-unchanged. NO UI change (Timeline/Inspector already 47-edit scoped). 2-PASS review: pass 1 = 0 Crit / 1 Important (a STALE COMMENT in the shape-easing branch falsely said the scalar/color/gradient/dash branches "stay root-resolved" after this slice routed them → rewritten; reviewer traced ALL ~20 branches: every read+write resolves the SAME scene, no half-routes, parity intact), pass 2 = CLEAN (0/0). 1154 unit + 71 e2e + typecheck + `src`/`e2e` lint green. **Every action that operates on a selected object/keyframe now routes to the active scene — the in-symbol editing surface is fully closed.** | `specs/2026-06-26-savig-m4-in-symbol-timeline-keyframes-design.md` · `plans/2026-06-26-savig-m4-in-symbol-timeline-keyframes.md` | `e98ee9c` |
| **SYMBOL LIBRARY THUMBNAILS (47d polish)** — the first 47d-polish slice; the AssetPanel "Symbols" section now shows a rendered SVG thumbnail of each symbol's content (was name+count text only). REUSE not duplication: `renderSvgDocument` (the canonical project→SVG renderer shared with export) gained an OPTIONAL `opts?: { viewBox?: string }` arg (default `0 0 W H` → export BYTE-UNCHANGED, regression-tested; `buildBundle` passes no opts). New UI helper `thumbnailSvg.ts::symbolThumbnailSvg(symbol, assets, meta)`: `sceneContentAABB(symbol.objects, assets, 0)` (null/degenerate → null → placeholder), synthetic `Project={meta,assets,objects:symbol.objects,audioClips:[]}` (no mutation), `renderSvgDocument(proj, { viewBox:'minX minY w h' })` — content-AABB viewBox because symbol members keep AUTHORED coords (NOT origin-normalized; verified vs createSymbol). `<SymbolThumbnail>` component memoized on `[symbol,assets,meta]` renders the svg via `dangerouslySetInnerHTML` (same sanitized output the export bundle ships — no NEW injection vector; reviewer confirmed) or a placeholder `<span>`; wired into each AssetPanel symbol `<button>` (span block, click-to-place/cyclic-disabled unaffected). GOTCHA FIXED: helper file named `thumbnailSvg.ts` NOT `symbolThumbnail.ts` — a case-insensitive FS (macOS) resolves `./symbolThumbnail` vs the component `./SymbolThumbnail` ambiguously → undefined component (caught in RTL: "Element type is invalid"). Nested-instance symbols render correctly (flattenInstances). Scope: thumbnails ONLY (drag-to-place / rename-delete-symbol deferred). 1-PASS review CLEAN (0/0; all 7 checks: parity byte-identical, helper correctness, no new XSS vector, no re-render storm, case-collision resolved, span-in-button valid, tests). 1160 unit + 72 e2e + typecheck + `src`/`e2e` lint green | `specs/2026-06-26-savig-m4-symbol-thumbnails-design.md` · `plans/2026-06-26-savig-m4-symbol-thumbnails.md` | `958f3a2` |
| **SYMBOL LIBRARY RENAME & DELETE (47d polish)** — the second 47d-polish slice; the AssetPanel "Symbols" rows can now be RENAMED + DELETED (was place/swap/count only). Two undoable store actions: **`renameAsset(assetId, name)`** (asset analogue of `renameObject` — trim; empty/whitespace/unchanged → no-op, no spurious commit; generic over asset kinds), **`deleteSymbol(symId)`** — IN-USE GUARD `countSymbolInstances(symId, project) > 0` (counts instances across root + EVERY symbol's objects[]; transitively covers "currently being edited" since entering a symbol requires an instance) → error toast + no-op; at 0 instances remove the symbol asset then CROSS-SCENE prune its now-orphaned vector/svg internal assets (`collectReferencedAssetIds(postDeleteProject)`, keep symbol/audio OR still-referenced — the phase-1/boolean predicate; a vector used only by the deleted symbol is pruned, one also used at root is KEPT). AssetPanel symbol row restructured from a bare `<button>` → a `<div>` row (mirrors the Layers `editingId` inline-rename): place `<button>` (keeps `data-testid`/click-to-place/cyclic-disabled), an uncontrolled rename `<input>` (commit on Enter/blur via renameAsset, Escape cancels — React doesn't re-fire onBlur after the input unmounts, so no spurious commit), + `Rename`/`Delete` sibling buttons (aria-labelled). NO engine-render change → parity intact (asset-metadata/array edits; a 0-instance symbol has no leaves). 1-PASS review CLEAN (0/0; all 6 checks: in-use guard airtight, prune on POST-delete project correct, rename no-op guard, parity, valid sibling markup, single-commit undo) + added a Minor-gap Escape-cancel RTL test the reviewer noted. 1167 unit + 73 e2e + typecheck + `src`/`e2e` lint green. DEFERRED: drag-to-place, swap-anchor-recompute, non-symbol-asset rename/delete | `specs/2026-06-26-savig-m4-symbol-rename-delete-design.md` · `plans/2026-06-26-savig-m4-symbol-rename-delete.md` | `a3c627c` |
| **SYMBOL DRAG-TO-PLACE (47d polish)** — the third 47d-polish slice; drag a symbol from the AssetPanel library onto the canvas to place an instance UNDER THE CURSOR (was click-to-place at content-centre only). New store action **`placeSymbolInstanceAt(symId, x, y)`** — a position-taking sibling of `placeSymbolInstance` (SAME cycle guard + active-scene routing via selectActiveObjects/selectActiveAssetId/commitActiveScene), but the instance gets `base = { ...DEFAULT_TRANSFORM, x: x - cx, y: y - cy }` (cx,cy = content-AABB centre) so the content-centre lands at (x,y) — a pure translation (reviewer verified `world.x = (x-cx)+cx+1·(cx-cx) = x`). `placeSymbolInstance` (click → authored coords, base 0) unchanged. AssetPanel symbol place `<button>` is now `draggable` + `onDragStart` puts symId on a CUSTOM MIME `dataTransfer.setData('application/x-savig-symbol', …)` (cross-component handoff — AssetPanel & Stage are separate, so a shared ref like the Layers DnD won't work). Stage `<svg>` gained `onDragOver` (preventDefault gated on the MIME so unrelated drags ignored) + `onDrop` (read symId, map client→scene via the EXISTING `clientToLocal` [inverts the content-group screen CTM → viewBox/pan/zoom-aware, returns active-scene coords at root AND in edit mode], call placeSymbolInstanceAt). Click-to-place still works (click ≠ drag); disabled cyclic buttons can't be dragged + the action's guard rejects anyway. NO engine-render change → parity intact. Playwright `dragTo` DID carry the dataTransfer (e2e: drop → instance count 1→2; no fallback needed). E2E COUNT GOTCHA: `[data-savig-object]` matches the instance CONTAINER + its composite leaf (2 per instance) → count composite leaves via `[data-savig-object*="/"]`. 1-PASS review CLEAN (0/0; all 5 areas: placement math, coord mapping, DnD wiring, parity, undo + tests). 1172 unit + 74 e2e + typecheck + `src`/`e2e` lint green. DEFERRED: swap-anchor-recompute, non-symbol rename/delete, drag-preview ghost | `specs/2026-06-26-savig-m4-symbol-drag-to-place-design.md` · `plans/2026-06-26-savig-m4-symbol-drag-to-place.md` | `79401f6` |
| **NON-SYMBOL ASSET RENAME & DELETE (47d polish)** — the fourth 47d-polish slice; imported svg/audio library assets can now be RENAMED + DELETED (was symbol-only), removing the asymmetry. New store action **`deleteAsset(assetId)`** (undoable) — return for a `symbol` (those use `deleteSymbol`); IN-USE GUARD = referenced by any OBJECT across scenes (`collectReferencedAssetIds(project)`) OR by any AUDIO CLIP (`audioClips.some(c => c.assetId === id)` — audio assets are referenced by clips, NOT objects, so the separate check is REQUIRED) → error toast + no-op; else remove the asset + commit. `renameAsset` (generic, from the symbol slice) reused unchanged. AssetPanel non-symbol rows restructured (like the symbol rows, shared `editingId` + `.symbolRow`/`.rowBtn` CSS): action button (`data-testid="asset-{id}"`, keeps svg→addObject/audio→addAudioClip) + inline rename `<input>` + Rename/Delete buttons shown ONLY for svg/audio (a `manageable` flag; per-shape VECTOR rows stay bare — vectors are 1:1 with objects, not library items; vector-list-cleanup deferred). NO engine-render change → parity intact (asset-metadata/array edits; the guard prevents dangling refs). 2-PASS review: pass 1 = 1 CRITICAL (the row gaining `Rename {name}`/`Delete {name}` buttons made SIX existing e2e tests' `getByRole('button', { name: 'box.svg' })` substring-AMBIGUOUS → Playwright strict-mode violation; confirmed by running export.spec.ts; FIXED with `exact: true` in export/svg-rotate/scale-handles/scale-edge-handles/uniform-scale/alt-scale-from-center; the earlier "exit 0" was a STALE dev-server bundle — re-ran on a fresh server to confirm), pass 2 = CLEAN (0/0; comprehensive scan found no other ambiguous selectors). E2E LESSON: adding an aria-labelled button that embeds an existing element's name silently breaks non-`exact` `getByRole(name)` selectors; and Playwright's webServer `reuseExistingServer` can serve a STALE bundle → kill stale dev servers before a definitive e2e run. 1181 unit + 75 e2e + typecheck + `src`/`e2e` lint green. DEFERRED: vector-list cleanup, swap-anchor-recompute | `specs/2026-06-26-savig-m4-asset-rename-delete-design.md` · `plans/2026-06-26-savig-m4-asset-rename-delete.md` | `ad8923a` |
| **SYMBOL DURATION MANUAL OVERRIDE (47c follow-up)** — the FIRST per-instance-timing follow-up; makes the previously-reserved `SymbolAsset.duration` field a real manual override of a symbol's effective loop/clip length (`0` = AUTO/intrinsic, `> 0` = the length). New engine helper **`symbolEffectiveDuration(asset) = asset.duration > 0 ? asset.duration : objectsMaxKeyframeTime(asset.objects)`** read at the SINGLE `flattenInstances` seam (`symbol.ts`, the `remapLocalTime` call) — so the override flows to BOTH preview (`computeFrame`) and export (`renderSvgDocument`) via the shared walker → PREVIEW==EXPORT preserved by construction (the field comment updated: now READ). New store action **`setSymbolDuration(symId, duration)`** — edits the GLOBAL symbol asset by id (a symbol is global; works from an instance selection), `Math.max(0,·)` clamp, no-op-if-unchanged guard (no spurious commit), undoable. Inspector "symbol duration" `NumberField` added to the per-instance "Symbol timing" panel (reads the symbol asset's duration via the already-resolved `asset`, label/title: "Affects every instance"). LIFTS the documented 47c 0-duration-collapse edge (a keyframe-less symbol now loops with a manual duration). FIRST intentional RENDER-BEHAVIOUR change since the in-symbol routing arc — but REGRESSION-SAFE: every existing symbol has `duration: 0` → falls to the intrinsic → byte-identical (a regression-baseline test pins it). `computeProjectDuration` deliberately NOT changed (it never accounted for symbol-internal keyframes; documented scope line). 1-PASS review CLEAN (0/0; reviewer confirmed the single seam, regression-safety, parity, the project-duration scope boundary; 1 sub-threshold redundant-lookup → simplified to reuse `asset`). 1189 unit + 76 e2e + typecheck + `src`/`e2e` lint green. DEFERRED (other 47c): loop modes (ping-pong/play-count-N/random-start) on `SceneObject.symbolTime` via `remapLocalTime`; keyframing `symbolTime`; symbol-instance duration in `computeProjectDuration` | `specs/2026-06-26-savig-m4-symbol-duration-override-design.md` · `plans/2026-06-26-savig-m4-symbol-duration-override.md` | `fa72ba6` |
| **SYMBOL PING-PONG LOOP MODE (47c follow-up)** — the SECOND per-instance-timing follow-up; a looping instance can now BOUNCE (play forward then backward, 0→dur→0…) instead of wrapping. New optional `SymbolTiming.pingPong?` (absent/false = wrap — the parity/regression baseline). `remapLocalTime` gains ONE branch gated on `loop && pingPong`: a triangle wave over a `2·dur` period — `m = t % (2·dur); return m <= dur ? m : 2·dur − m` (C⁰-continuous at the apex `m===dur`→dur and the wrap seam `m→2·dur`→0; reviewer-verified). Same SINGLE-seam, default-off-gate, shared-`flattenInstances` pattern the duration override proved → PREVIEW==EXPORT by construction; pingPong without loop falls through to one-shot (nothing to bounce). `setSymbolTiming` merges pingPong via a CONDITIONAL spread `...((partial.pingPong ?? cur.pingPong) ? { pingPong: true } : {})` — absent by default, set-false CLEARS the field (`false ?? cur` is `false`, not nullish), so existing `symbolTime` objects stay byte-clean (a `??`→`||` regression would silently keep it true — now covered by a set-false test). Inspector "ping-pong" checkbox in the Symbol-timing panel (`data-testid="symbol-pingpong"`). 2-PASS review: pass 1 = 0 Crit / 1 Important (set-false + preserve paths uncovered → leaves the `??`→`||` regression undetectable; added 2 store tests + an e2e uncheck round-trip), pass 2 = CLEAN (0/0; the set-false test proven to catch the `\|\|` swap). 1196 unit + 77 e2e + typecheck + `src`/`e2e` lint green. DEFERRED (other 47c): play-count-N, random-start, keyframed `symbolTime`, symbol-instance duration in `computeProjectDuration` | `specs/2026-06-26-savig-m4-symbol-pingpong-design.md` · `plans/2026-06-26-savig-m4-symbol-pingpong.md` | `b0c4899` |
| **ASSETPANEL VECTOR-ASSET FILTER (47d cleanup)** — the AssetPanel top "library" list used a symbol-BLACKLIST (`kind !== 'symbol'`) which swept per-shape `vector` assets (1:1 with their `SceneObject`; `duplicate.ts` clones per copy — NOT library items) into the list, where the binary onClick (`kind==='svg' ? addObject : addAudioClip`) MIS-ROUTED a vector click to `addAudioClip` (a bogus audio clip). Switched to a reusable-kind WHITELIST `kind === 'svg' || kind === 'audio'` (`nonSymbols`→`libraryAssets`): vectors never render a row, symbols keep their own section, and the onClick can no longer mis-route. Whitelist is FORWARD-SAFE (a new non-reusable kind is excluded by default). Dropped the now-tautological `manageable` flag (every row is svg/audio → rename/delete always render). UI-LIST-ONLY → engine/store/serialization untouched → preview==export trivially intact. 1-PASS review CLEAN (0 Crit/0 Imp, 1 Minor [the tautological flag] cleaned up; reviewer confirmed the whitelist is exhaustive vs the 4-kind union, no stale-closure, tests assert vector-unlisted + audio-click-routes). 1197 unit + 77 e2e + typecheck + `src`/`e2e` lint green | `specs/2026-06-26-savig-m4-assetpanel-vector-filter-design.md` · `plans/2026-06-26-savig-m4-assetpanel-vector-filter.md` | `e93901f` |
| **MULTI-SELECT BOUNDS SPAN GROUPS & INSTANCES (47b polish)** — the multi-select scale-handle box (`groupBounds` useMemo in Stage) built its union with `objectAABB`, which returns null for symbol INSTANCES (`resolveObjectAnchor` falls through for asset kind `symbol`) and GROUP containers (no `assetId`) → a marquee/shift-select including an instance or group produced a TOO-SMALL box that clipped them out. The codebase already had the right dispatcher — `entityAABB` (group→`groupAABB`, instance→`instanceAABB`, plain→`objectAABB`) — but the multi-select branch was never migrated (single-select already special-cased both). Extracted a pure, unit-testable **`multiSelectionAABB(ids, objects, assets, time)`** in `snapping.ts` (same union loop but via `entityAABB`; skips hidden/locked; `groupBBox([])`→null) and wired Stage's `>1` branch to it (dropped the now-unused `assetsById` memo dep — reviewer-confirmed no stale closure). EDITOR-CHROME ONLY (selection handles) → never touches `flattenInstances`/`computeFrame`/`renderSvgDocument` → parity untouched by construction; plain-only selections byte-identical (`entityAABB`≡`objectAABB` for vectors). Cycle-safe (each selected item gets an independent fresh-Set traversal). 1-PASS review CLEAN (0 Crit/0 Imp; reviewer traced the dep change, cycle guards, plain-selection equivalence + tests [instance maxX 110-not-10, group span, hidden/locked→null]). Reviewer NOTE (pre-existing, NOT a regression, logged as follow-up): Stage's per-object dashed selection-outline (line ~1871) also uses `objectAABB` → groups/instances get no INDIVIDUAL dashed rect in a multi-selection (only the outer `groupBounds` rect, now correct). 1200 unit + 77 e2e + typecheck + `src`/`e2e` lint green | `specs/2026-06-26-savig-m4-multiselect-instance-bounds-design.md` · `plans/2026-06-26-savig-m4-multiselect-instance-bounds.md` | `140f90d` |
| **SYMBOL PLAY-COUNT-N LOOP MODE (47c follow-up)** — the THIRD per-instance-timing follow-up (after duration-override + ping-pong); a looping instance plays N full cycles then HOLDS the final frame (e.g. a flourish that bounces 3× and stops). New optional `SymbolTiming.playCount?` (absent / 0 = loop forever). `remapLocalTime` gains a finite-cycle clamp on the SAME single-seam/default-off pattern: after hoisting the one-shot early-return (`if (!loop) return min(t,dur)`), `if (playCount > 0) { cycle = pingPong ? 2·dur : dur; if (t >= playCount·cycle) return pingPong ? 0 : dur; }` then the existing ping-pong/wrap branches. A "cycle" = a full timeline pass (wrap `dur`; ping-pong `2·dur` there-and-back); exhausted holds the cycle END (wrap → `dur` last frame; ping-pong → `0`, where the Nth bounce lands; at the boundary the exhaust branch and the natural mod AGREE → no discontinuity, reviewer-verified). The body restructure is byte-identical for `playCount` absent/0 → parity + 47c regression tests intact. `setSymbolTiming` int-clamps (`Math.max(0, Math.floor(·))`) + conditional-spreads playCount (`...(pc && pc>0 ? {playCount:pc} : {})` — 0 clears → loop forever; same byte-clean discipline as pingPong). Inspector "play count" NumberField (0 = loop forever) + RTL + e2e. 1-PASS review CLEAN (0 Crit/0 Imp; reviewer traced the exhaustion boundary `>=`, the refactor equivalence for every prior case, the merge clamp; 1 trivial Minor [no playCount=1 wrap test] added). 1209 unit + 78 e2e + typecheck + `src`/`e2e` lint green. DEFERRED (other 47c): random-start (per-instance phase), keyframed `symbolTime`, symbol-instance duration in `computeProjectDuration` | `specs/2026-06-26-savig-m4-symbol-playcount-design.md` · `plans/2026-06-26-savig-m4-symbol-playcount.md` | `d7e8ec9` |
| **SWAP-SYMBOL ANCHOR RECOMPUTE (47d follow-up)** — `swapSymbol` repointed only `assetId`, keeping the OLD symbol's content-centre anchor → a different-sized new symbol rendered off-centre and the R·S pivot was wrong → the instance JUMPED. Fix: recompute the anchor to the NEW symbol's content-AABB centre (`sceneContentAABB(newSym.objects, …)`, already imported, same convention as `placeSymbolInstance`) and shift the translation by `Δ = oldAnchor − newCentre` so the pivot world position (`base + anchor`, since `world(a)=T+a`) is INVARIANT — no jump. CRUCIAL completeness: x/y tracks store ABSOLUTE values (`sampleObject` ignores `base` when a track exists) AND a motion path overrides x/y entirely, so `Δ` is applied to `base` AND every `tracks.x`/`tracks.y` keyframe AND every `motionPath.path.nodes[].anchor` (in/out handles are relative offsets → unchanged); only x/y move, not scale/rotation. Empty new symbol (`box` null) → bare `assetId` repoint (old behaviour preserved); Δ=0 between equal-box symbols → byte-identical. Mutation-safe (fresh objects at every level), undoable. Store-only → NO render change → parity untouched. 2-PASS review: pass 1 = 0 Crit / 2 Important — (a) a FALSE-POSITIVE "recompute untested" (reviewer missed the appended `describe('swapSymbol anchor recompute')` block whose leaves DO set `shapeBase` → real centres (5,5)/(20,20), asserting anchor→20, pivot-invariance, keyframe shift — each falsifiable), and (b) a REAL gap: motion-path position not compensated → FIXED by translating the path node anchors + a test (nodes [[100,50],[140,90]]→[[85,35],[125,75]]); pass 2 = CLEAN (0/0; both confirmed, math + handle-completeness + mutation-safety re-traced). 1214 unit + 78 e2e + typecheck + `src`/`e2e` lint green. DEFERRED: preserve-custom-anchor-on-swap option | `specs/2026-06-26-savig-m4-swap-anchor-recompute-design.md` · `plans/2026-06-26-savig-m4-swap-anchor-recompute.md` | `4449c80` |
| **DASHED SELECTION-OUTLINE SPANS GROUPS & INSTANCES (47b polish)** — the follow-up the multi-select-bounds review logged. In a multi-selection Stage draws a thin dashed rect per object (`data-testid="selection-outline-{id}"`); it used `objectAABB` → null for group containers + symbol instances → those members got NO individual outline (only the outer `groupBounds` box). ONE-LINE swap to the existing `entityAABB(o, project.objects, project.assets, time)` dispatcher (`project` = the edit-scoped active scene, correct at root AND in-symbol; `project.assets` global) — exactly what the outer box now uses. Plain vector/svg outlines byte-identical (`entityAABB`≡`objectAABB` for them; `assetsById.get` ≡ `assets.find` on the same list); `!o.hidden` guard + locked-member drag-offset untouched; EDITOR-CHROME ONLY → parity untouched. 1-PASS review CLEAN (0 Crit/0 Imp; reviewer confirmed `project` === `selectEditProject` semantics, plain-object equivalence, negligible perf, the instance-outline test). 1215 unit + 78 e2e + typecheck + `src`/`e2e` lint green | `specs/2026-06-26-savig-m4-dashed-outline-entityaabb-design.md` · `plans/2026-06-26-savig-m4-dashed-outline-entityaabb.md` | `e01eb74` |
| **SYMBOL RANDOM-START PHASE (47c follow-up)** — the FOURTH per-instance-timing follow-up (after duration-override, ping-pong, play-count-N); an instance can start partway into its internal timeline so clones of one symbol desync (the "field of bobbing grass, each on its own phase" effect). New optional `SymbolTiming.phase?` (absent/0 = start at frame 0) added to the elapsed internal time in `remapLocalTime`: `const t = (parentTime − startOffset) * speed + (phase ?? 0)` — a head-start in INTERNAL-clock seconds (added AFTER the speed scale so it's speed-independent), flowing through the existing one-shot/wrap/ping-pong/playCount branches unchanged. Same single-seam/default-off/shared-`flattenInstances` pattern → preview==export; absent → `+0` → byte-identical. `setSymbolTiming` clamps `Math.max(0,·)` + conditional-spreads (`...(ph && ph>0 ? {phase:ph} : {})` — 0 clears). Inspector "phase" NumberField (title "desyncs clones") + RTL + e2e. 1-PASS review CLEAN (0 Crit/0 Imp; reviewer traced the phase-after-speed unit choice, the `t<=0` pre-start interaction, parity, the merge; 2 sub-threshold combos [phase+startOffset, phase+playCount] added as tests). 1226 unit + 79 e2e + typecheck + `src`/`e2e` lint green. DEFERRED: a "randomize phase" button (nondeterministic UI sugar on this mechanism); keyframed `symbolTime` | `specs/2026-06-26-savig-m4-symbol-randomstart-design.md` · `plans/2026-06-26-savig-m4-symbol-randomstart.md` | `360dc8f` |
| **SYMBOL-INSTANCE INTERNAL ANIMATION IN computeProjectDuration (47c follow-up)** — `computeProjectDuration` (auto mode) counted an instance's OWN transform keyframes but NOT the symbol's internal animation → a symbol that animates internally on an otherwise-static root yielded duration 0 (playhead range + exported frame count didn't cover the visible animation). New pure `instanceTimelineEnd(obj, assetsById)` maps the instance's internal length to the parent timeline: `startOffset + active/speed`, `active = !loop ? internal : playCount>0 ? playCount*cycle : cycle`, `cycle = pingPong ? 2*internal : internal`, `internal = symbolEffectiveDuration(symbol)` (the SAME intrinsic length the renderer's remap uses); folded as `max(instanceTimelineEnd over project.objects)` (the flat array → grouped instances covered). MOVED `symbolEffectiveDuration` symbol.ts→duration.ts (its natural home; avoids a cycle since symbol.ts already imports duration.ts; behaviour-neutral, symbol.test re-imports from ./duration). **`computeProjectDuration` is the SHARED timeline length for BOTH preview playback/seek AND export frame count → extending it moves both together → preview==export preserved by construction.** Regression-safe: no-instance projects → instanceTimelineEnd returns 0 → byte-identical; manual durationMode early-returns. INCIDENTAL CORRECTION: in-symbol seek is now bounded by the symbol's content length (was unbounded because root duration was 0) → 2 in-symbol copy/paste-keyframe tests retargeted from seek(5)/seek(t1+7) to in-range seeks. 1-PASS review CLEAN (0 Crit/0 Imp; reviewer verified every instanceTimelineEnd case, the 4 shared callers move together, the move-neutrality, the documented nested-instance-recursion deferral matching the renderer; 1 sub-threshold [phase not subtracted → safe overestimate, the spec's documented choice]). 1231 unit + 79 e2e + typecheck + `src`/`e2e` lint green. DEFERRED: nested-instance internal recursion; phase-aware shortening | `specs/2026-06-26-savig-m4-instance-project-duration-design.md` · `plans/2026-06-26-savig-m4-instance-project-duration.md` | `d077d91` |
| **MULTI-SELECT MOVE PREVIEWS NODE-LESS CONTAINERS (47b polish)** — during a multi-select MOVE drag the preview loop only wrote to `nodes.get(it.id)`, which is null for a GROUP (composes onto children at compute time) and a symbol INSTANCE (renders as `instId/childId` leaves) → those members FROZE in place until pointer-up (commit was always correct). Generalised the move loop to mirror the already-correct group-handle SCALE/ROTATE loops: compute the member's translate `xf`, then `node ? setAttribute(xf) : isGroup ? previewGroupChildren(proj, id, time, xf) : isSymbolInstance ? previewInstanceChildren(proj, obj, time, {x,y,scaleX,scaleY,rotation,opacity})`. Plain-object path byte-identical (node present → setAttribute with the SAME xf); `setDragOffset` (dashed-outline follow) untouched. IMPERATIVE PREVIEW ONLY (never the committed project / flattenInstances / export) → preview==export untouched; previewInstanceChildren filters to the instance's own `id/` leaves so a mixed multi-drag never clobbers siblings; commit re-render supersedes the preview. 1-PASS review CLEAN (0 Crit/0 Imp; reviewer confirmed the dispatch mirrors scale/rotate, plain-object equivalence, parity, no stale-preview risk, the instance-leaf test). 1232 unit + 79 e2e + typecheck + `src`/`e2e` lint green | `specs/2026-06-26-savig-m4-multiselect-move-container-preview-design.md` · `plans/2026-06-26-savig-m4-multiselect-move-container-preview.md` | `7ce0a88` |
| **BOOLEAN-OP KEYBOARD SHORTCUTS (slice 46 follow-up)** — boolean ops were buttons-only; added `Cmd/Ctrl+Shift+U/S/I/E` → union/subtract/intersect/exclude in `useKeyboard`. The store `booleanOp(op)` already SELF-GATES (`eligible.length < 2` and `rings.length === 0` both early-return), so the handler calls it unconditionally — safe no-op when ineligible, no duplicated eligibility logic. Branches sit in the `mod` group AFTER the `isEditable` guard and BEFORE the no-modifier tool-key `switch`, each `preventDefault()+return` so `Cmd+Shift+S/E` don't fall through to `star`/`ellipse`; plain `s`/`e` still select tools (regression-tested); no clash with existing `mod` combos (Z/D/G/C/X/V/[/], Shift+Z/G). DOC CAVEAT: `Ctrl+Shift+I` is the browser DevTools toggle on Win/Linux Chrome/Edge, consumed at the browser-process level BEFORE the page sees it — `preventDefault` can't reclaim it, so Intersect's shortcut is ALWAYS shadowed there (the button is the fallback; U/S/E unaffected). No store/engine change → parity untouched. 1-PASS review: 0 Crit / 1 Important (the comment said "may be shadowed" — sharpened to "always", reviewer explicitly said NO code change needed) + an uppercase-key test added. 1238 unit + 79 e2e + typecheck + `src`/`e2e` lint green | `specs/2026-06-26-savig-m4-boolean-shortcuts-design.md` · `plans/2026-06-26-savig-m4-boolean-shortcuts.md` | `fde519d` |
| **CENTER SELECTION ON CANVAS (align-to-artboard)** — align (slice 43) only aligns objects to EACH OTHER (≥2); there was no align-to-ARTBOARD. New pure **`computeCenterOnFrame(items, frameW, frameH)`** in `align.ts` shifts the whole selection by ONE delta so its combined `groupBBox` centre lands on `(frameW/2, frameH/2)` (relative offsets preserved; ≥1 item; EPS/empty → `[]`). Store **`centerOnCanvas()`** reads `meta.width/height`, runs it through the existing `alignItemsUpdates`→`setObjectsTransforms` pipeline (one undo step, active-scene write). Inspector "Center on canvas" button (aria-labelled, ⊡) in BOTH the single-object and multi-select panels (works for a single object too, unlike align). Layout op → no render change → preview==export; purely additive. INHERITS the align-family model: the `autoKey` gate (normal objects position through keyframes) and root-scope — like align/distribute it's a safe NO-OP inside a symbol (`alignItemsUpdates` reads root `project.objects`); documented, not claimed as in-symbol. 1-PASS review CLEAN (0 Crit/0 Imp; reviewer verified the single-delta geometry, family consistency, parity, fresh-state tests; 2 sub-threshold notes [both-axis keyframe write; in-symbol no-op] = consistent-with-family). 1244 unit + 79 e2e + typecheck + `src`/`e2e` lint green. DEFERRED: edge-align-to-artboard (left/right/top/…); making the align family active-scene-aware (would then need the symbol's own dims) | `specs/2026-06-26-savig-m4-center-on-canvas-design.md` · `plans/2026-06-26-savig-m4-center-on-canvas.md` | `64fc78b` |
| **DISTRIBUTE BY CENTERS** — `distributeSelected` distributes by equal GAP; added the complementary equal-CENTERS mode (centroids evenly spaced — the right choice for differently-sized boxes). New pure **`computeDistributeCenters(items, axis)`** (≥3; sort by center; first & last centers fixed; `step = (lastC−firstC)/(n−1)`; `d = firstC + i·step − center`; first/last deltas are algebraically 0 so the EPS guard filters them cleanly) + store **`distributeCentersSelected(axis)`** (verbatim structural mirror of `distributeSelected`, same `alignItemsUpdates`/`setObjectsTransforms`) + two Inspector buttons (⇿/⇳) next to the gap-distribute ones, gated by the existing `canDistribute` (movable ≥3). Layout op → no render change → preview==export; gap-distribute untouched; inherits the family autoKey/root-scope. 1-PASS review CLEAN (0 Crit/0 Imp; reviewer verified the centroid algebra, gap-vs-centers distinction, family consistency, tests). 1249 unit + 79 e2e + typecheck + `src`/`e2e` lint green. DEFERRED: numeric spacing INPUT; distribute relative to the artboard | `specs/2026-06-26-savig-m4-distribute-centers-design.md` · `plans/2026-06-26-savig-m4-distribute-centers.md` | `60ee73d` |
| **GROUP LOCK CASCADE** — locking a group now makes its whole subtree inert for editing (was: `.locked` gates checked only the object's OWN flag, so a child of a locked group stayed editable). New pure engine helper **`isLockedInTree(obj, objectsById)`** in `groupTransform.ts` — a structural MIRROR of `isRenderHidden`: own-lock short-circuit, then a cycle-guarded `parentId` walk that gates only on locked GROUP ancestors. Routed the INTERACTION-gating `.locked` sites through it across store (duplicate/delete/setProperties/nudge/setObjectsTransforms/alignItemsUpdates), Stage (all 6 handle memos + pointer-down + group scale/rotate drag-start + marquee + motion overlay + drag-offset outline), Timeline (row-select + 6 keyframe-edit gates), Inspector (movable-for-align count + single-object Create Symbol disable), LayersPanel (draggable + row-click + drop-target reject onto a locked subtree). TWO CLASSES triaged: OWN-STATE/visual sites LEFT as `o.locked` (the lock toggle action, the LayersPanel lock icon + `aria-pressed`, the `.locked` row-CSS in Layers + Timeline — mirrors how the existing isRenderHidden cascade applies to RENDER but panels still show OWN `o.hidden`); top-level-only filters (`!o.parentId`) LEFT (cascade ≡ own). PERF: the id `Map` is built once per action (hoisted before hot loops; loop-invariant where `objects` is reassigned) and `useMemo`'d in every component. BYTE-COMPATIBLE for ungrouped objects (`isLockedInTree(o,map)===o.locked`). EDITOR-CHROME/interaction ONLY → no engine-render change → parity untouched. 2-PASS review: pass 1 = 0 Crit / 1 Important (`lockById` rebuilt every render incl. every playback frame in Timeline/Inspector/Layers → memoized to match Stage's pattern; 2 Minor noted [multiSelectionAABB still own-lock; pre-existing assetsById dep] — left, low-impact), pass 2 = CLEAN (0/0). 1255 unit + 81 e2e (2 new lock-cascade specs) + typecheck + `src`/`e2e` lint green. (NOTE during this slice 8 in-symbol-edit e2e in `symbols.spec.ts` appeared to fail — later diagnosed as a thumbnail/selector collision, NOT this slice; fixed in `293ccf5`, see below.) DEFERRED: instance-in-group drag-preview (fiddly) | `specs/…` (in plan doc) · `plans/2026-06-26-savig-m4-lock-cascade-and-layout.md` | `841475c` |
| **EDGE-ALIGN-TO-ARTBOARD** — `computeAlign` aligns objects to EACH OTHER and `computeCenterOnFrame` centers on the artboard; this adds per-EDGE align to the artboard (left/right/h-center/top/bottom/v-center to the frame). New pure **`computeAlignToFrame(items, edge, frameW, frameH)`** (per-item delta: left→0, right→frameW, hcenter→frameW/2, top→0, bottom→frameH, vcenter→frameH/2; EPS-filters no-ops) + store **`alignToCanvas(edge)`** (verbatim `centerOnCanvas` shape through `alignItemsUpdates`→`setObjectsTransforms`; autoKey-gated, root-scope safe-no-op-in-symbol) + 6 Inspector buttons in BOTH panels (works for a single object too, like center-on-canvas). Layout op → no render change → preview==export; purely additive. 1-PASS review CLEAN (0 Crit/0 Imp; reviewer verified per-edge geometry, family consistency, no aria-label collision [RTL exact-by-default, e2e exact:true]). 1262 unit + e2e green. DEFERRED: per-edge align to a custom margin | shares `plans/2026-06-26-savig-m4-lock-cascade-and-layout.md` | `8f50828` |
| **DISTRIBUTE BY NUMERIC SPACING** — `computeDistribute`/`computeDistributeCenters` DERIVE the spacing from the selection extent; this adds an EXACT pixel-gap mode. New pure **`computeDistributeSpacing(items, axis, gap)`** (≥2; sort by lo edge; first fixed; each subsequent box placed `gap` after the previous box's hi edge via a cursor walk) + store **`distributeSpacingSelected(axis, gap)`** (family shape) + Inspector numeric `<input min=0>` (local state, default 10, clamped non-negative) + H/V buttons gated by the existing `canDistribute` (movable ≥3, family parity; helper allows ≥2). Layout op → no render change → preview==export. 2-PASS review: pass 1 = 0 Crit / 2 Important (e2e asserted only equal gaps → couldn't distinguish from equal-gap distribute → strengthened to assert the rightmost rect PACKS left + `gap1<40`; negative gap allowed → `min=0`+`Math.max(0,·)` clamp), pass 2 = CLEAN (0/0). 1269 unit + e2e green. DEFERRED: distribute relative to the artboard | shares `plans/2026-06-26-savig-m4-lock-cascade-and-layout.md` | `35d08ae` |
| **FIX — in-symbol e2e thumbnail/selector collision** — `symbols.spec.ts`'s in-symbol-edit tests had been timing out (60s "element is not enabled", apparent ~7min/spec). ROOT CAUSE (diagnosed via a throwaway spec that `.evaluate`'d the matched element's ancestor chain + a `force:true` click that worked in 2ms → actionability false-positive, not perf): the symbol-library THUMBNAILS (`958f3a2`) render symbol content through `renderSvgDocument`, which emits `data-savig-object` (the runtime animates exported SVGs by querying it → must stay). Those thumbnail groups live in the AssetPanel, so the tests' bare `[data-savig-object]` / `:not([data-savig-object*="/"])` selectors matched them too — inflating count assertions and, in EDIT MODE, resolving `.first()` to a group inside a DISABLED symbol button (cycle-guard) → not-enabled hang × 8 tests. FIX (test-only, no app change): scope every object query to `section[aria-label="Stage"] [data-savig-object…]`, restoring the pre-thumbnail Stage-only DOM calibration. **Full e2e now 83 passed in ~12s.** LESSON: a previously-working selector that starts timing out on "not enabled" → suspect a NEW DOM source of the same attribute (a static thumbnail/export reusing the editor renderer inherits its `data-savig-object` hooks), not the environment; scope ambiguous selectors to their region. | (test-only) | `293ccf5` |
| **KEYFRAMED SYMBOL TIME-REMAP — CORE (47c, the LAST 47c item)** — After-Effects "Time Remapping": keyframe an instance's INTERNAL playhead directly so it speeds up / slows down / FREEZES / REVERSES over the parent timeline. Chosen over animating `startOffset` (indirect: rate `speed·(1−so′)`) or `speed` (needs `∫speed dt`, stateful) — a direct time curve is integral-free and reuses the scalar keyframe machinery. New `SceneObject.symbolTimeTrack?: Keyframe[]` (`time`=parent-local sec, `value`=internal-clock sec). At the SINGLE `flattenInstances` `childTime` seam: non-empty track ⇒ `childTime = Math.max(0, interpolate(track, localTime))` (slope=rate, flat=freeze, downward=reverse; `interpolate` clamps out-of-range parent times to the curve ends, and `sampleObject` clamps out-of-range internal times) — SUPERSEDES the constant `symbolTime`; absent ⇒ byte-identical (parity, 47a test untouched). `instanceTimelineEnd` returns the track's max keyframe time when present (covers preview seek-range + export frame count). Store `toggleSymbolTimeRemap` (seeds identity `[0→0,D→D]`, or `[0→0]` for a 0-duration symbol; disable clears) + `setSymbolTimeRemap(value)` (upsert at the snapped playhead) — active-scene-routed via `commit(replaceObjectInScene(…))`, undoable. Inspector "time remap" checkbox + "internal time" field (upserts at playhead, clamped display); the constant timing fields disable while a track is present. Integral-free pure lookup → preview==export exact by construction. 2-PASS review: pass 1 = 0 Crit / 1 Important (negative-value clamp untested → added), pass 2 CLEAN (+ a Minor display-clamp polish). 1282 unit + 81 e2e green. | `specs/2026-06-27-savig-m4-keyframed-symbol-time-remap-design.md` · `plans/2026-06-27-savig-m4-keyframed-symbol-time-remap.md` | `e6e797c` |
| **KEYFRAMED SYMBOL TIME-REMAP — TIMELINE MANIPULATION (47c)** — the manipulation surface for the remap track, mirroring the dash/progress per-type pattern (a `symbolTimeTrack` is a plain `Keyframe[]` like `dashOffsetTrack`). New `RemapKeyframeRef` + `selectedRemapKeyframe` state + `KeyframeClip` `'remap'` case + `selectRemapKeyframe`/`removeSelectedRemapKeyframe`; a `remap` branch added to the SHARED `copyKeyframe`/`retimeSelectedKeyframe`/`pasteKeyframe`/`setSelectedKeyframeEasing`/`deleteSelectedKeyframe` (no half-route — full op parity); every selection-clearing site (`TRANSIENT_DEFAULTS`, each `select*` clear-list, `selectObject`/`selectObjects`) also clears the remap ref. Timeline renders a `remap-keyframe-{id}-{time}` diamond row (lock-gated, `--color-remap` token in both themes) wired to select + the shared drag-to-retime. NO engine-render change → parity intact. 2-PASS review: pass 1 = 0 Crit / 1 Important (missing Timeline-unit diamond test — every other track type has one → added), pass 2 CLEAN. 1287 unit + 84 e2e green. **Keyframed symbol time-remap COMPLETE → 47c per-instance timing fully done.** DEFERRED: looping a remap curve; step/hold easing; graphical curve editor. | shares the time-remap spec/plan | `9b8abcd` |
| **ALIGN FAMILY ACTIVE-SCENE-AWARE** — align/distribute/distribute-centers/distribute-spacing/center-on-canvas/edge-align were ROOT-scoped (a documented no-op inside a symbol); now they operate on the ACTIVE scene. `alignItemsUpdates` reads `selectActiveObjects(s)` (assets stay global) instead of root `project.objects`; new `activeSceneDims(s)` returns the edited symbol's `width`/`height` in edit mode (else root `meta`) for center/edge-align. The WRITE path (`setObjectsTransforms`) was ALREADY active-scene-aware (lock-cascade slice), so only the READ + dims needed fixing — all six actions flow through the one `alignItemsUpdates` seam. Editor layout op → no engine-render change → parity intact; ROOT byte-unchanged (`selectActiveObjects`≡root objects, `activeSceneDims`≡meta when not in a symbol). 1-PASS review CLEAN (0/0; reviewer verified root-identity, active-scene read + global assets, crash-safe dims, both discriminating in-symbol tests; 1 pre-existing Minor noted [alignItemsUpdates uses objectAABB not entityAABB → groups/instances excluded from alignment, true at root too]). 1289 unit green. | (deferral, no separate spec) | `a9283c9` |
| **PASTE-AT-CURSOR** — `paste` (Cmd/Ctrl+V) now drops the copy under the pointer when it's over the Stage (was always a fixed diagonal `DUP_OFFSET`). New module-level `Stage/stageCursor.ts` ref holds the pointer in ACTIVE-SCENE coords (chosen over a Zustand field to avoid per-mousemove subscriber churn; `store.ts` already imports from `components/Stage/*`); the Stage `<svg>` `onPointerMove` writes it via `clientToLocal`, `onPointerLeave` clears it. `paste()` reads `getStageCursor()`: non-null ⇒ compute the clipboard's combined bbox (`objectAABB` per entry at the snapped time, `groupBBox`-unioned) and `delta = cursor − centre`, paste each object with `duplicateObject(…, 0)` + a uniform `base` shift by `delta` (relative positions preserved, centre lands at the cursor); null ⇒ the existing `DUP_OFFSET` path, BYTE-IDENTICAL (RTL/e2e have no Stage pointer events). All-group/instance clipboards (null AABBs) fall back to `DUP_OFFSET` (no crash). Active-scene coords → works inside a symbol. Editor-only → parity intact. 1-PASS review CLEAN (0/0; reviewer verified no-cursor byte-identity, uniform-delta math, capture-drag bubbling, fallback, parity). 1290 unit + 85 e2e green. DEFERRED: animated-x/y paste shifts only base (pre-existing, same as DUP_OFFSET). | (deferral, no separate spec) | `58e88e1` |
| **SCALE POSITION-SNAPPING — group + single-object scale (1/2)** — resize/scale drags now snap the moving corner/edge to other objects' edges & centers + the artboard (with the move-snap guides), incl. uniform (Shift) & from-center (Alt). KEY: snap the POINTER onto (mode-constraint ∩ target line), not the factor — `applyScaleHandleDrag`'s own projection (R(-rot); uniform→diagonal) is then a no-op, so it stays UNCHANGED and the edge lands on the guide. New pure `Stage/scaleSnap.ts`: `snapScalePoint` (free per-axis — degenerate-AABB `computeSnap`, applies dx/dy only on dragged axes) + `snapScaleAlongSegment` (uniform→`oC→cC` diagonal / from-center→`aC→cC` ray — project onto the segment, slide along it to a nearby guide, with a 2D-distance threshold guard so a non-45° slide doesn't over-snap). Wired into the GROUP scale (axis-aligned, free per-axis) + SINGLE scale (`scaleRef`→`applyScaleHandleDrag`, free/uniform/from-center) drags; targets (other `entityAABB` + artboard) built on pointer-down; guides via `setSnapGuides`, cleared on pointer-up AND on a mid-drag snap-off. Gated on `snapEnabled` + (single) rotation≈0. Editor-chrome only → parity intact; snap-off/no-target byte-identical. 2-PASS review: pass 1 = 0 Crit / 2 Important (missing 2D-distance threshold guard → off-axis over-snap; guides not cleared on mid-drag snap-off → both fixed + 2 tests), pass 2 CLEAN. 1297 unit + 86 e2e green. v1 LIMIT: a ROTATED single object doesn't snap (no clean factor for off-axis scaling). NEXT (2/2): rect/ellipse RESIZE (`resizeRef`, needs a stage↔object-local conversion). | `specs/2026-06-27-savig-m4-scale-snapping-design.md` · `plans/2026-06-27-savig-m4-scale-snapping.md` | `954f7f6` |
| **SCALE POSITION-SNAPPING — rect/ellipse RESIZE (2/2)** — extends the snap to the geometry-RESIZE machine (`resizeRef`→`applyHandleResize`), so dragging a rect/ellipse edge/corner handle snaps the moving edge to other objects' edges/centers + artboard with the move-snap guides; free / uniform (Shift) / from-center (Alt) / uniform+from-center (Shift+Alt). KEY DIFFERENCE from slice 1: the resize pointer is in OBJECT-LOCAL bbox coords (via `handleGroupRef`'s CTM) but targets are STAGE-space → snap in stage space then convert back. `toStage`/`toLocal` = compose `handleGroupRef` CTM with `contentRef` CTM (one `matrixTransform` per hop — jsdom can't chain). MODIFIER ROUTING differs from the scale machine: resize from-center pivots about the geometric CENTRE (not the anchor), so 3-way — Shift-only→`fixedCorner→draggedCorner` diagonal; Shift+Alt→`centre→draggedCorner` ray; else (free OR Alt-only-corner OR edge handle)→free per-axis `snapScalePoint`; `isCorner` gates the segment branches so edge handles never over-constrain. Reuses slice-1's `snapScalePoint`/`snapScaleAlongSegment` UNCHANGED; the back-converted local point lands on `applyHandleResize`'s own `projectParam` segment → its projection is a no-op. Targets (other `entityAABB` + artboard) built on resize pointer-down; guides cleared on pointer-up + mid-drag snap-off. Gated `snapEnabled` + rotation≈0 + CTM available. Editor-chrome only → parity intact; snap-off/no-target/rotated byte-identical (existing resize tests now route THROUGH the snap path yet pass unchanged = identity round-trip proof). 1-PASS review CLEAN (0 Crit/0 Important; reviewer verified round-trip inverses, 3-way routing matches `applyHandleResize` branches incl. Alt-only→per-axis + edge-handle exclusion, byte-identity gates, guide clearing, null-safety). 1297 unit + 87 e2e green. **SCALE/RESIZE POSITION-SNAPPING COMPLETE (both slices).** Still deferred: rotated-object scale snap (no clean off-axis factor); rotate angle-snap. | shares the scale-snapping spec/plan | `2ba4b3f` |
| **ROTATE-HANDLE ANGLE-SNAP (magnetic 45°)** — rotate drags now magnetically click to 45° multiples when the snap toggle is ON (consistent with move/scale/resize snap; NO Shift modifier — toggle snap off for free rotation). New pure `rotateHandle.ts` `snapAngle(deg, step, threshold)` → `{ angle, snapped }`: nearest multiple via `Math.round(deg/step)*step` (handles negatives + wrap, e.g. 358°→360°; `-0` normalized), snaps only within `ANGLE_SNAP_DEG=5°` of an `ANGLE_SNAP_STEP=45°` multiple. Wired into BOTH rotate move handlers: SINGLE object snaps the ABSOLUTE `next` rotation (object aligns to 0/45/90 regardless of start angle); GROUP snaps the `theta` DELTA swept about the centre (the selection rotates in 45° steps; each member ends at `orot + snappedDelta`). Gated on `snapEnabled` (`let` refactor keeps the snap-off path byte-identical). The magnetic lock IS the feedback — a dedicated rotation guide/readout overlay is DEFERRED. 1-PASS review CLEAN (0 Crit/0 Important; reviewer verified the nearest-multiple formula incl. negatives/wrap/-0, snap-off byte-identity, group-delta-vs-absolute semantics, single-object absolute snap, pointer-up commit flow). 1303 unit + 88 e2e green. **The move/scale/resize/rotate snapping family is now COMPLETE.** | (deferral, no separate spec) | `2836fa7` |
| **ROTATE ANGLE READOUT HUD** — the deferred feedback for rotate angle-snap: a live `<text data-testid="rotate-readout">` shown at the cursor during any rotate drag, reading the current angle and BOLD + `data-snapped` when the magnetic 45° snap engages. New `rotateHud` state set per-move in BOTH rotate handlers (SINGLE = absolute orientation normalized to [0,360); GROUP = signed sweep delta normalized to (−180,180]), cleared on both rotate pointer-ups. Rendered in the content `<g>` (content coords; `1/zoom`-scaled font/stroke; `paintOrder=stroke` panel-colored outline so it reads on any background). Per-move setState is the ESTABLISHED drag pattern here (move/scale/resize handlers already call `setSnapGuides`/`setDragOffset` each move; the React re-render doesn't clobber the imperative `setAttribute` previews — those elements carry no `transform` prop). 1-PASS review CLEAN (0 Crit/0 Important; reviewer traced both normalizations incl. the 180°/360° boundaries, HUD lifecycle = only-set-in-2-handlers + cleared on both window-registered pointer-ups, no double-render interference, coord correctness, e2e validity). 1303 unit + 88 e2e green (e2e asserts the readout shows "45°"/`data-snapped` mid-drag + clears on release). | (deferral, no separate spec) | `8564ad5` |
| **NODE-DRAG SNAP (node tool)** — dragging a path's node ANCHOR with the Node tool now snaps it to other objects' edges/centers + the artboard, reusing `computeSnap` + the shared `snapGuides` overlay (same target set as move/scale/resize — `entityAABB` per other object + artboard, built on node pointer-down, self-excluded). The node is in OBJECT-LOCAL coords (overlay CTM) but targets are STAGE-space → snap the node's stage position (`clientToLocal` → point-AABB `computeSnap`) then convert back via new `stageToObjectLocal` (content→screen via `contentRef` CTM, screen→local via `overlayGroupRef` CTM inverse; one matrixTransform/hop). KEY: a POINT snap needs NO rotation gate (unlike resize's per-axis edge snap) — we snap the absolute stage point then invert through the overlay CTM, correct under any rotation/scale. Bezier control HANDLES never snap (`usePathTools` now returns the active `grab` so the caller tells anchor from handle). Byte-identical when snap off / no-match / handle-grab: the adjusted local is applied ONLY when a guide matched (`r.guideX!==null||r.guideY!==null`), else the raw `local` passes through untouched (no round-trip float drift). Guides + targets cleared on node pointer-up. 1-PASS review CLEAN (0 Crit/0 Important; reviewer verified byte-identity predicate, inverse-CTM composition, stale-grab safety via per-render ref reassign, self-exclusion, lifecycle, types). 1303 unit + 89 e2e green. DEFERRED: snapping to other paths' VERTICES/nodes (only bbox targets today); snapping bezier handles. | (deferral, no separate spec) | `d97db50` |
| **EQUAL-SPACING SNAP + DIMENSION GUIDES** — dragging a single object near the equidistant point between its immediate row/column neighbors now snaps it to EQUAL gaps and draws the gap dimension lines (end-ticks + px label) — Figma-style "smart spacing", v1. New pure `spacingGuides.ts` `computeSpacingSnap(moving, others, threshold) → {dx,dy,guides}`: per axis, among `others` that VERTICALLY (resp. horizontally) overlap `moving`, pick the nearest left/right (resp. top/bottom) flanking neighbor (`o.maxX <= moving.minX` etc. → gaps always ≥0, overlapping neighbors excluded → no bogus negative-gap snap), `delta=(gapR−gapL)/2`; within threshold → shift to centre + emit two guide segments at the gap value. Wired into the SINGLE-object move drag AFTER edge-snap: spacing fills an axis ONLY where edge-snap left `guideX`/`guideY` null (edge wins, never double-applies — a fired axis has both `snap.dx≠0` and `guide≠null`); computed on the edge-snapped bbox so guides land at the final spot; guides filtered to the applied axes. New `spacingGuides` state rendered in the content `<g>` (`1/zoom`-scaled line+ticks+`paintOrder` label), cleared on snap-off + drag pointer-up. The artboard AABB can never be a flanking neighbor (always overlaps the mover) so it never participates. Byte-identical when snap off / no match. 1-PASS review CLEAN (0 Crit/0 Important; reviewer verified delta sign+equalization, neighbor reduce, overlap exclusion, edge-wins/no-double-apply, lifecycle, render keys, types). 1309 unit + 90 e2e green. v1 LIMITS: single-object move only (not multi/scale); centred-between-IMMEDIATE-neighbors only (no match-an-existing-gap distribution); no spacing for the multi-select drag. | (deferral, no separate spec) | `1a6db47` |
| **SPACING v2 — MATCH-AN-EXISTING-GAP DISTRIBUTION** — extends `computeSpacingSnap` so a dragged object also snaps when its gap to a neighbor equals an EXISTING gap elsewhere in the same row/column (Figma distribution), on top of v1 centring. Pure-helper-only change (Stage wiring + render UNCHANGED — already consume `{dx,dy,guides}`). Refactored the H/V passes into ONE generic `axisSnap(moving,others,threshold,overlaps,lo,hi,cross,make)` (lo/hi=edges on the snap axis, cross=perp centre, make=segment builder); H pass = overlapsV/minX/maxX/cyOf, V = overlapsH/minY/maxY/cxOf. Candidates: (a) CENTRE between immediate L&R `δ=(gapR−gapL)/2`; (b) MATCH-LEFT `δ=e.gap−gapL`; (c) MATCH-RIGHT `δ=gapR−e.gap` — for each existing adjacent gap `e` among sorted mover-excluded mates (consecutive pairs where `lo(b)≥hi(a)`). SMALLEST `|δ|` within threshold wins; a match emits the mover's snapped-gap segment + a segment over the matched existing gap. 1-PASS review CLEAN (0 Crit/0 Important; reviewer verified both match deltas/signs make the gap exactly `e.gap`, guide endpoint lengths, generic axis wiring with no h/v mix-up, overlap-guarded existing pairs, min-shift tiebreak, v1 untouched). 1313 unit + 91 e2e green. Still v1-LIMIT: single-object move only (not multi/scale). | (deferral, no separate spec) | `d702915` |
| **HOLD-TO-BYPASS-SNAP MODIFIER (Cmd/Ctrl)** — holding Cmd (mac) / Ctrl (win/linux) DURING a drag momentarily suppresses ALL snapping (move/scale/resize/rotate/node/spacing); release re-enables. One `snapActive = snapEnabled && !(e.metaKey||e.ctrlKey)` computed once at the top of the `onMove` pointermove handler replaces the 8 inline `snapEnabled` reads (perl replace-all, all 8 inside onMove → byte-identical when no modifier held; read-once is semantically identical to per-site within a synchronous event). CAVEAT (intended): `onObjectPointerDown` already uses Cmd/Ctrl at PRESS time as the add-to-selection gesture (`toggleObjectOrGroup`+return), so a MOVE drag can't START with the modifier — press it AFTER the drag begins; HANDLE drags (scale/resize/rotate/node pointer-downs don't read metaKey) can hold from the start. 1-PASS review CLEAN (0 Crit/0 Important; reviewer verified byte-identity, replacement scoped to onMove only, cross-platform predicate, non-flaky e2e that presses Meta mid-drag + asserts no guide while a real move happened, types). 1313 unit + 92 e2e green. **Snapping family + ergonomics now comprehensive: move/scale/resize/rotate(+readout)/node/equal-spacing/distribution + Cmd-bypass.** | (deferral, no separate spec) | `044eae2` |
| **SNAP-TO-GRID + visible grid (move drags)** — independent grid feature: new persistent store prefs `gridEnabled:false`/`gridSize:20` (own toggle, NOT folded into snapEnabled; in the create body so they survive newProject) + `toggleGrid`/`setGridSize` (clamp `max(1,round)`). Pure `gridSnap.ts` `snapAABBToGrid(moving,gridSize) → {dx,dy}` snaps the bbox TOP-LEFT to the nearest lattice multiple — NO threshold (grid is absolute; `!(gridSize>0)` guard catches 0/NaN). Wired into SINGLE + MULTI move drags as the LAST layer: object-edge snap → spacing → grid, where grid fills only axes not already claimed (`claimX/claimY` = edge guide OR spacing guide present; grid never double-moves a claimed axis). Cmd/Ctrl bypass applies (`gridActive = gridEnabled && noBypass`). Refactored single-move to an `adjX/adjY` accumulator — byte-identical to the spacing slice when grid off. Faint accent grid overlay rendered behind objects (after `<defs>`, `pointerEvents:none`, `0.5/zoom` stroke), gated `floor(w/sz)+floor(h/sz) ≤ 400` (caps DRAWING only; snapping always works; grid lines are drag-independent → per-move setState produces no DOM churn). Timeline: Grid toggle (mirrors Snap) + a `Grid size` number input shown when on. 1-PASS review CLEAN (0 Crit/0 Important; reviewer verified grid math both branches, claim/priority logic + h↔X/v↔Y orientation, byte-identity grid-off, newProject survival, cap gates draw-not-snap, e2e). 1318 unit + 93 e2e green. v1 LIMIT: MOVE drags only (resize/scale handles don't grid-snap yet); single show+snap toggle (no separate show-grid). | (deferral, no separate spec) | `13da189` |
| **SNAP-TO-GRID for SCALE + RESIZE handles** — extends grid-snap (was move-only) to the three handle machines. New pure `snapPointToGridAxes(p,sxAxis,syAxis,claimedX,claimedY,gridSize)` snaps the dragged corner/edge POINT on each dragged + UNCLAIMED axis (object-snap wins per axis). Applies only in the FREE per-axis case — the uniform(Shift)/from-centre(Alt) cases project onto a diagonal/ray so a per-axis grid nudge is skipped (`constrained`). GROUP-SCALE (always free) snaps `corner` (content); SINGLE-SCALE snaps px/py (content, fed to applyScaleHandleDrag); RESIZE snaps the stage point before `toLocal` (gate widened to `(snapActive||gridActive)` so grid works with object-snap off). rotation≈0 gate + Cmd/Ctrl bypass as before. **REVIEW (1 Important, fixed→re-review CLEAN):** single-scale `constrained` first read `shift||alt`, which over-skipped grid for the ALT-ONLY corner when object-snap is OFF — but `applyScaleHandleDrag` from-centre is FREE per-axis (`scaleHandles.ts` 76-86, no diagonal projection) when `!uniform`; only the object-SNAP alt path projects onto the anchor→corner ray. Fix: `constrained = isCorner && (shift || (alt && snapActive))` — grid valid for alt+snap-off, skipped for alt+snap-on. (resize alt-only was already correct: free per-axis in both snap+apply, `constrained = isCorner && shift`.) 1322 unit + 94 e2e green. v1 LIMIT unchanged scope now covers move+scale+resize; uniform/from-centre handle drags still don't grid-snap (deferred — would need grid-on-diagonal projection). | (deferral, no separate spec) | `8217f38` |
| **NODE-VERTEX SNAP — snap to other paths' nodes** — extends node-drag snapping (was bbox edges/centers + artboard) so a dragged path ANCHOR also snaps onto another path's actual NODE anchor points. Two new pure `snapping.ts` exports: `pathContentVertices(obj,asset,time)` → a path's node anchors in CONTENT coords (maps each node local→content via `content = anchor + R(rot)·S(scale)·(local−anchor) + base`, the SAME composition as `transformedAABB`; includes boolean-op COMPOUND-RING nodes — same object transform/anchor — added after review); `snapToVertices(p,verts,thr)` → nearest vertex within a 2D threshold else null. New `nodeVertexRef` built on node grab (other paths' vertices, self excluded; grouped paths skipped = deferred); the node-drag move handler PRIORITIZES vertex snap (pins BOTH axes → crosshair guide via `setSnapGuides({x:vtx.x,y:vtx.y})`) over the edge/center AABB snap, falling back BYTE-IDENTICALLY when no vertex is near; cleared on pointer-up. Coordinate-consistent (vertices + dragged `stage` both content coords; `stageToObjectLocal` lands the snapped vertex in the dragged path's local frame). **REVIEW (1 Important, fixed→re-review CLEAN):** compound-ring nodes were omitted → added (boolean compound paths now full snap targets). 1328 unit + 95 e2e green. DEFERRED: same-path other-vertex snap; grouped paths' vertices; bezier-handle snap. | (deferral, no separate spec) | `742c721` |
| **BOOLEAN OPS ON GROUPS** — a selected GROUP is now a valid boolean operand (was: ≥2 non-group vector shapes only), collapsed to the UNION of its vector-leaf descendants and treated as ONE operand → intersect/subtract/exclude see the MERGED group, not its parts (distinguished by the test `big ∩ group{s1,s2 disjoint} == s1∪s2`, which would be ∅ if flattened). ENGINE `boolean.ts`: new `operandWorldGeom(project,obj,time)` (non-group→`objectToWorldPolygon`; group→`pc.union` of leaf world polygons via `collectVectorLeaves`, cycle-guarded) feeds the reduce (`booleanOp` operand type widened to `PcPolygon|PcMultiPolygon`); correct because `toWorld` already walks the parent chain (leaf-in-group → right world coords). STORE `booleanOp`: `vectorLeavesOf`/`descendantIdsOf` helpers; eligibility = `vectorLeavesOf(o).length>0` (group counts as one operand); STYLE inherited from the topmost vector LEAF (`eligible.flatMap(vectorLeavesOf)` max zOrder — a group has no asset); REMOVE the whole group subtree + prune ALL removed objects' assets (symbol/audio/referenced-elsewhere still protected). INSPECTOR `canBool` mirrors via recursive `hasVectorLeaf`. 1-PASS review CLEAN (0 Crit/0 Important; reviewer verified all 4 ops' union-operand semantics, empty-group filter, cycle-guard tradeoff [reparent guards cycles], empty-string-assetId prune is benign, topmost-leaf style+zOrder comparability, subtree removal leaves no dangling refs, Inspector↔store eligibility agree, active-scene routing). 1331 unit + 96 e2e green. v1 LIMITS: SVG-asset operands still excluded; a group's non-vector children (instances/audio) are consumed+discarded; curve-preserving + animated boolean still deferred. | (deferral, no separate spec) | `0fc9da0` |
| **CURVE-PRESERVING BOOLEAN RESULTS (v1, leaf operands)** — boolean results KEEP original bezier/ellipse curvature on untouched edges (a circle returns ~4 curved nodes, not a 64-gon); only intersection seams become corners (Illustrator/Figma model). APPROACH (chosen over a Paper.js swap): provenance ON TOP OF `polygon-clipping`, no new dep. NEW engine module `geom/boolean-curves.ts` — cubic primitives (`evalCubic`/`reverseCubic`/`splitCubicRange` direction-aware De Casteljau/`projectToCubic` 24-seed+ternary-refine/`isStraightCubic`), `cubicsToRing` (flatten for clip), `classifyVertex` (project a clipped vertex onto operand cubics → provenance or null=corner), `segmentsToPathData` (OutSeg[]→nodes; seam node carries `in` from incoming + `out` from outgoing = tangent-correct kink; straight cubics→corners), `reconstructRing` (VERBATIM rebuild from original segs when a ring is one untouched operand [disjoint-union / shape-fully-inside] + GENERAL run-walk: rotate to a run boundary, group maximal same-(opIdx,segIdx) runs → split sub-cubic, `ptOf` anchors every provenance vertex at its ON-CURVE projection so line/stitch segments share endpoints exactly with split cubics). `boolean.ts`: `operandCubicsWorld` (leaf outline→world cubics; rect=4 straight, ellipse=4 KAPPA quadrants, path=`prev.out||cur.in` rule; maps 4 control pts through existing `toWorld`, affine-exact; []for groups); `booleanOp` rewritten — leaf operands carry provenance, group/SVG operands keep `operandWorldGeom` flat union + NO provenance entry (their verts never project-match → corners, faceted as before); `opIdx` keyed (NOT geoms-position) so geoms/operands length decouple safely; bbox-relative `tol=max(1e-4,diag*1e-4)`; per-ring try/catch → faceted `ringToPathData` fallback (never throws/corrupts). STORE FIX (caught by e2e, not units): `booleanOp` result `shift` rebuilt nodes as `{anchor}` only, DROPPING in/out → spread `...n` keeps the (translation-invariant offset) handles so curves reach the DOM. PARITY: all-corner (rect/path-square) inputs reconstruct to corner nodes, byte-faithful. SUBAGENT-DRIVEN (inline impl + reviewer subagent per task + OPUS whole-branch review, all clean/ready-to-merge); 8 TDD tasks. v1 SCOPE/LIMITS: LEAF operands only (groups stay faceted — group pre-union is a 2nd clip that loses provenance); SVG operands still excluded; result still a baked snapshot (animated boolean deferred); seam approximation = curved run ends ≤1/16-segment short of the true intersection, bridged by a short straight stitch (curvature exact away from seams, minutely straight right at them — documented). 1363 unit + 97 e2e green. | `specs/2026-06-27-savig-m4-boolean-curve-preserving-design.md` · `plans/2026-06-27-savig-m4-boolean-curve-preserving.md` | `f5d4477` |
| **COMPOUND-RING NODE EDITING (v1)** — the Node tool can now edit a boolean result's compound rings (holes / disjoint pieces), not just its primary outline. Extends node ADDRESSING from a flat `selectedNodeIndex` into `asset.path` to a `(selectedNodeRing, selectedNodeIndex)` pair across `path` + `compoundRings` (ring 0 = primary, ring k = `compoundRings[k-1]`). Full parity: move/handles/insert/delete/smooth/join on any ring. SELECTORS `selectEditableRings` (=[primary morph-sampled, ...static compoundRings]) + `selectActiveRingPath`. STORE `setRingPathData(ring,path,structural?)`: ring 0 delegates to the existing morph-aware `setPathData` (shapeTrack/primitive-detach reused verbatim); ring k writes `compoundRings[k-1]` DIRECTLY (no shapeTrack) — the two write paths are fully DISJOINT (meet only at the pure even-odd render combine), which is what makes "holes static under morph" a safe deferral (regression-pinned). delete/insert(ring)/toggleSmooth/join route through the active ring; `setSelectedNodeEasing` ring-0-gated. DRAG: new pure `pickRingTarget(rings,local,tol)` (discriminated `RingTarget` union; handle-beats-anchor, lowest-ring-wins) + `usePathTools` `working: { ring, path }` committing `setRingPathData`. OVERLAY (Stage): `selectedPath.rings[]` renders every ring's nodes (ring-0 testid `node-${i}` = e2e parity, holes `node-${r}-${i}`); `transform`/anchor stay PRIMARY-anchored so a hole-node drag never shifts the object frame; insert hit-test scans all rings; morph-only surfaces (easing markers, correspondence overlay, Inspector `nodeEasingCtx`) gate to ring 0. Reuses `pathEdit`/`pathHitTest` helpers UNCHANGED (ring-agnostic). PARITY: ring-0 editing byte-identical (existing node-edit unit + e2e green); `selectedNodeRing` only set on a non-null index (selectNode/insertNode) so the ~15 null-reset sites need no ring reset (stale ring under null index is harmless; guarded by `selectActiveRingPath ?? null` + `k>=rings.length` no-op). SUBAGENT-DRIVEN (inline impl + reviewer subagent per task [Tasks 2 and 3+4 one fix pass each] + OPUS whole-branch review = Ready-to-merge); 5 TDD tasks. v1 LIMITS: compound rings STATIC under morph (a ShapeKeyframe stores one path); whole-ring add/delete out of scope; fractional-anchor transform-stability mid-drag render test = accepted follow-up (inert for unrotated boolean results). 1379 unit + 98 e2e green. | `specs/2026-06-27-savig-m4-compound-ring-node-editing-design.md` · `plans/2026-06-27-savig-m4-compound-ring-node-editing.md` | `7e0ecc5` |
| **INSTANCE-IN-GROUP DRAG PREVIEW** — dragging a GROUP (move/scale/rotate via bbox handles) now live-previews its ENTIRE subtree: leaf children, symbol-INSTANCE children (which render as flattened `instId/…` leaves, no DOM node), AND nested groups. BUG: old `previewGroupChildren` used a transform-STRING prefix applied per direct child `if (!node) continue` — so instance + nested-group children (no node) were silently skipped and FROZEN until commit; only leaf children followed. FIX: converge the group preview onto the recompute-frame model the instance preview already used. NEW pure engine helper `groupDescendantIds(objects, groupId): Set<string>` (downward parentId walk, cycle-guarded via the out-set, `out.delete(groupId)` so it excludes the group even under a corrupt cycle). Stage: extracted `previewSubtree(proj, containerId, base: Transform2D, time, ownRenderId)` (container carries in-progress base w/ tracks stripped → shared `computeFrame` [= the commit path, so preview matches by construction] → filter to `ownRenderId` → `applyFrameToNodes`); `previewInstanceChildren` delegates w/ `startsWith(instId/)`; `previewGroupChildren` rewritten to `groupDescendantIds` + filter `id => descendants.has(id.split('/')[0])` (split maps a composite renderId — incl. doubly-nested `outerInst/innerInst/leaf` — back to its proj.objects-level producer). KEY: filter applies ONLY the dragged container's own leaves → a mixed multi-select drag never reverts a sibling's preview (same guarantee instance preview gives); `path`/`transform`/anchor stay PRIMARY-derived so a child drag can't shift the frame. The 3 call sites (scale/rotate/move) pass the SAME Transform2D base as the `previewInstanceChildren` call beside each (byte-matches the commit formula → preview≡commit). Commit path (`setObjectsTransforms`) UNCHANGED. PARITY: leaf-only group preview unchanged in behavior (only the leaf node's transform string FORMAT changes concatenated→flattened; existing `toContain('scale(2')` test still passes). SUBAGENT-DRIVEN (inline impl + reviewer subagent per task + OPUS whole-branch review = Ready-to-merge); 3 TDD tasks (Task 1+2 batched, Task 3 the fix). v1: per-move frame recompute is the accepted instance-preview cost; nested-group MOVE integration test = accepted follow-up (mechanism unit+integration covered, parity-by-construction). 1384 unit + 98 e2e green. | `specs/2026-06-27-savig-m4-instance-in-group-drag-preview-design.md` · `plans/2026-06-27-savig-m4-instance-in-group-drag-preview.md` | (merge of d80b348) |
| **ANIMATED BOOLEAN — SLICE 1: live-boolean geometry** (milestone slice 1 of 3) — a NON-destructive live boolean: `SceneObject.boolean = { op, operandIds }` whose rendered path is recomputed EVERY FRAME by clipping its operand objects, so animating an operand animates the boolean. Mirrors MORPH's pattern: a field on the object makes geometry dynamic, computed per-frame via the existing `pathD` seam. NEW `resolveBooleanRings(project, booleanObj, time): PathData[]` (resolve operandIds from project.objects → existing `booleanOp`; [] when <2 operands). `BoolOp` MOVED to types.ts (so `SceneObject.boolean` references it without a types↔boolean cycle); boolean.ts imports-not-re-exports it (engine index `export * from` BOTH → re-export would be a duplicate-export conflict dropping the name, breaking store.ts). `flattenInstances` skips operands (`consumed` set) so they animate+select but don't draw. `computeFrame` boolean branch sets `item.pathD = pathToDRings(rings…)` at `leaf.localTime`. **ARCH KEY (corrected mid-impl): the editor paints frames IMPERATIVELY via `applyFrame(nodes,project,time)` [Stage.tsx:321] = computeFrame every render (NOT just RAF playback) — so computeFrame MUST compute the boolean (editor playback would otherwise FREEZE it) → the runtime bundle now ships polygon-clipping; the bake-at-export alternative is INFEASIBLE (incompatible w/ live playback). `applyFrameToNodes` sets `d` but NOT fill-rule → the Stage React branch sets the initial `d` + `fill-rule=evenodd` (else subtract booleans fill their holes).** Regenerated `runtimeSource.generated.ts` (now bundles polygon-clipping, ~136KB). PARITY: non-boolean objects byte-identical (obj.boolean absent → existing path on both render paths). SUBAGENT-DRIVEN (inline impl + reviewer per task + OPUS whole-branch review = Ready-to-merge); 4 TDD tasks. v1: ROOT-SCENE only (boolean-in-symbol deferred); boolean node = identity transform + world-space path (movable transform deferred); NO authoring UI (programmatic, slice 2) / NO standalone-export initial markup (slice 3). Accepted follow-ups: per-frame clip caching (perf), self-ref/nested-boolean operand rejection (slice 2 authoring), active-scene-vs-root resolver unification (at boolean-in-symbol). 1391 unit + 98 e2e green. | `specs/2026-06-27-savig-m4-animated-boolean-slice1-design.md` · `plans/2026-06-27-savig-m4-animated-boolean-slice1.md` | (merge of e472f6d) |
| **ANIMATED BOOLEAN — SLICE 2: authoring** (milestone slice 2 of 3) — Alt+(boolean button or `Cmd/Ctrl+Shift+U/S/I/E`) creates a LIVE boolean (slice 1 node) instead of the destructive baked one; plain click stays destructive. `booleanOp(op)` → `booleanOp(op, opts?: { live?: boolean })`: the live branch (inserted after `eligible`, before the destructive gate, with `return`) creates a `SceneObject.boolean` node from selected NON-GROUP vector leaves that are NOT themselves live booleans (filter `!isGroup && !o.boolean && asset.kind vector`); self-gates `<2`; path-typed `VectorAsset` (style from TOPMOST-zOrder leaf, empty fallback path), identity transform, name `Animated <Op> N`, `operandIds` in SELECTION order; KEEPS operands (`[...activeObjects, obj]`, no remove/prune); one commit → undoable. **ROOT-SCENE GATE (`activeAssetId === null`): live render reads root `project.objects`, but the buttons are enabled inside a symbol-edit scene → a live boolean authored in a symbol would render empty; so inside a symbol the live branch FALLS THROUGH to the scene-agnostic destructive boolean (found by the OPUS whole-branch review).** UI: 4 Inspector buttons + 4 shortcuts pass `{ live: e.altKey }` (+ title "Alt: animated"). Destructive path byte-identical (only the signature line changed). **E2E SIGNAL: slice-1 RENDER-HIDES operands, so a live Union shows 1 `[data-savig-object]` (the result) — the kept operands persist in the LAYERS tree (3 rows) which distinguishes live (3) from destructive (1).** SUBAGENT-DRIVEN (inline impl + reviewer per task [1 fix pass] + OPUS whole-branch review [1 Important: root-scene guard → fixed+re-reviewed clean]); 2 TDD tasks. v1: groups + nested live booleans deferred (excluded); `canBool` reflects DESTRUCTIVE eligibility so an Alt+click w/ <2 live-eligible leaves silently no-ops (documented; render-time Alt-aware disabled state = slice 3). NEXT: slice 3 = standalone-export markup (renderSvgDocument boolean-aware) + bundle measure + editing-operands/nested/perf. 1400 unit + 99 e2e green. | `specs/2026-06-27-savig-m4-animated-boolean-slice2-design.md` · `plans/2026-06-27-savig-m4-animated-boolean-slice2.md` | (merge of a383615) |
| **ANIMATED BOOLEAN — SLICE 3a: standalone export** (milestone slice 3, sub-slice a) — closes the EXPORT gap: a standalone `.savig` (`renderSvgDocument`) emitted NO `<path>` child for a live-boolean object → the embedded runtime (which only sets `d` on EXISTING nodes) had nothing to animate → blank. Now the exported initial markup emits `<path fill-rule="evenodd" d="<time-0 clip>">` (operands flatten-skipped → absent), so the runtime animates `d` per frame, matching the editor. (1) `renderShapeToSvg` gains optional 9th param `forceEvenOdd?: boolean` → `(forceEvenOdd || hasRings)` drives the `fill-rule="evenodd"` attr (`d`-routing still `hasRings`-based); existing 8-arg callers byte-identical. (2) `renderSvgDocument` path branch: when `obj.boolean`, `boolRings = resolveBooleanRings(project, obj, 0)` → `boolRings[0]` framePath, `boolRings.slice(1)` compoundRings, `!!obj.boolean` forceEvenOdd; an empty/degenerate result emits a `<path fill-rule="evenodd" d=""/>` placeholder (mirrors the pre-existing MORPH empty-path fallback). **ARCH KEY: the runtime (`frame.ts`) makes the IDENTICAL `resolveBooleanRings`+`pathToDRings` call at time 0 and only ever sets `d` (never `fill-rule`) → export must BAKE evenodd into the initial markup, and the export time-0 `d` is byte-identical to what the runtime recomputes at time 0 (no drift, Opus-verified against frame.ts).** A boolean's path ALWAYS carries evenodd even if time-0 has no hole (a later frame's hole then cuts correctly). NO runtime-bundle change (runtime already computes booleans, slice 1). PARITY: non-boolean export byte-identical (obj.boolean absent → boolRings null + forceEvenOdd false → original branch). SUBAGENT-DRIVEN (inline impl + reviewer per task [1 Important+1 Minor fix pass: comment the `resolveBooleanRings` ≥3-node/[] invariant at the `boolRings![0]` site + tighten the union test to assert evenodd AND non-empty `d="M…"` on the SAME `<path>`] + re-review clean + OPUS whole-branch review = Ready-to-merge, 0 Critical/Important); 2 TDD tasks. v1: ROOT-SCENE only; group/nested-boolean operands (3b), editing-operand UX + Alt-aware button-disabled (3c), per-frame clip caching (3d) deferred. 1405 unit green. | `specs/2026-06-27-savig-m4-animated-boolean-slice3a-export-design.md` · `plans/2026-06-27-savig-m4-animated-boolean-slice3a-export.md` | (merge of c1a6d37) |
| **ANIMATED BOOLEAN — SLICE 3b: group + nested-boolean operands** (milestone slice 3, sub-slice b) — a live boolean may now take a GROUP (= union of its vector-leaf descendants, mirroring the destructive `0fc9da0`) or another LIVE BOOLEAN (its own per-frame result) as ANY operand: rendered, animated, authored (Alt), exported. Everything funnels through the ONE seam `resolveBooleanRings`. **ENGINE (`boolean.ts`):** extract `booleanResultGeom` = the raw `PcMultiPolygon` clip result BEFORE the ring-reconstruct tail (`booleanOp` = that + the unchanged tail → byte-identical `PathData[]`); new `resolveBooleanGeom(project,boolObj,time,visited)` returns a nested boolean operand's RAW multipolygon — **holes preserved as polygon nesting, NOT its rendered even-odd ring list (feeding rings back as positive polys would FILL holes)**; `operandWorldGeom` gains a leading `if (obj.boolean) return resolveBooleanGeom(...)` branch + threaded `visited`; `operandCubicsWorld` guards `obj.boolean`; `resolveBooleanRings`/`booleanOp` thread an optional `visited: Set<string> = new Set()` **CYCLE GUARD (copied per recursion → self-ref + mutual A↔B → `[]`, no false pruning of DAG-shaped nesting where two operands share a deeper boolean)**. Groups already resolved in `booleanOp` (prior destructive path) → NO new group engine code. **FLATTEN (`symbol.ts`):** `flattenInstances`'s `consumed` set expands a GROUP operand's whole subtree via `groupDescendantIds` (else the group's leaves draw directly AND feed the union — double-render); nested-boolean operands need no special case (their ids + own operandIds already collected); O(1) `rootById` map. **RUNTIME BUNDLE REGENERATED** (unlike 3a — `frame.ts`→`resolveBooleanRings`+`flattenInstances` are bundled, so a standalone export ANIMATES group/nested operands, not just the time-0 markup). **AUTHORING (`store.ts`):** the live-branch operand filter becomes `eligible` (`vectorLeavesOf(o).length>0` — groups-with-leaves + booleans + leaves; empty groups + SVG excluded on both UI & engine); style descends to the topmost-zOrder reachable leaf via `flatMap(vectorLeavesOf)` (a group/boolean has no direct shape asset); 2 slice-2 self-gate tests FLIP from no-op to creating a boolean. `Inspector.canBool`/`hasVectorLeaf` already counted groups + booleans (a boolean's asset is a vector path) → NO Inspector change. PARITY: leaf-only booleans + all non-boolean scenes byte-identical (new branches gated on `obj.boolean`/`isGroup`, optional trailing `visited`). SUBAGENT-DRIVEN (inline impl + reviewer per task [T1/T2/T4; T2 Minor perf fix; T3 generated bundle + T5 test-only → whole-branch] + OPUS whole-branch review = Ready-to-merge, 0 Critical/Important); 5 TDD tasks. DEFERRED: faceted-only (no curve provenance for group/nested — intentional), **a boolean nested INSIDE a group operand → empty** (`collectVectorLeaves` treats it as a leaf → empty fallback path; benign, fails-to-empty), curve-preserving group/nested, SVG operands; remaining slice-3: 3c editing-operand UX + Alt-aware button-disabled, 3d per-frame clip caching. 1412 unit green. | `specs/2026-06-28-savig-m4-animated-boolean-slice3b-group-nested-operands-design.md` · `plans/2026-06-28-savig-m4-animated-boolean-slice3b-group-nested-operands.md` | (merge of f98fb52) |
| **ANIMATED BOOLEAN — SLICE 3c: operand discoverability on canvas** (milestone slice 3, sub-slice c) — a live boolean's operands are render-hidden (`flattenInstances` `consumed`), so they were invisible on the editor canvas (the Stage renders from `flattenInstances` too) — visible only as Layers-panel rows. They were ALREADY editable once selected (the selection overlay uses `entityAABB` regardless of render; moving an operand re-clips the boolean live), so the ONLY gap was **seeing + clicking operands on the canvas**. **ENGINE (`boolean.ts`):** new `operandWorldRings(project,obj,time): PathData[]` — a single operand's world outline, uniform for a leaf (its shape) / GROUP (union of its leaves) / nested boolean (its result); normalizes `operandWorldGeom`'s `PcPolygon \| PcMultiPolygon` via an `isMulti = Array.isArray(geom[0]?.[0]?.[0])` probe (PcPolygon innermost is a number; PcMultiPolygon innermost is a Pair=array) → flat ring list + `>=3`-node filter; mirrors `resolveBooleanRings`'s shape. **STAGE (`Stage.tsx`):** an `operandGhosts` memo (gated `activeAssetId===null` + non-null `selectedId`; active boolean = selected-if-`.boolean` else the first root object whose `boolean.operandIds.includes(selectedId)` → ghosts appear when the boolean OR an operand is selected, so siblings stay visible) renders one faint dashed `<path data-testid="operand-ghost-<id>" data-operand-of="<boolId>">` per operand. **ARCH KEY: the boolean's own `<g>` carries NO per-object transform — its path is drawn from `resolveBooleanRings` WORLD coords directly in the content `<g>` — so world-coord ghosts (operandWorldRings) align with no transform math.** `fill="transparent"` + `pointerEvents:'all'` makes the whole area clickable; `onPointerDown` → `stopPropagation()` (no canvas-deselect) + `selectObject(id)`; the operand then edits via the EXISTING handles → re-clips live. Ghosts draw above the boolean fill / below the handles. PARITY: any non-boolean/non-operand selection (or inside a symbol, or no selection) → zero ghosts, canvas byte-identical. **Alt-aware button-disabled sub-item = ALREADY SATISFIED by 3b** (live eligibility == `canBool`) → dropped from scope (YAGNI). SUBAGENT-DRIVEN (inline impl + reviewer per task [T1/T2; each Minor-only, both test-strengthened] + e2e + OPUS whole-branch review = Ready-to-merge, 0 Critical/Important); 3 TDD tasks + e2e (`boolean-live-operands.spec.ts`: create live boolean → 2 ghosts → click ghost → nudge → result re-clips). DEFERRED: distinct styling for the selected operand's ghost; node-editing through a ghost (use the Node tool after selecting); 3d per-frame clip caching; the 3b deferrals. 1420 unit green. | `specs/2026-06-28-savig-m4-animated-boolean-slice3c-operand-editing-design.md` · `plans/2026-06-28-savig-m4-animated-boolean-slice3c-operand-editing.md` | (merge of 4270bfd) |
| **ANIMATED BOOLEAN — SLICE 3d: per-frame clip caching — CLOSED, NOT BUILT** (milestone slice 3, sub-slice d) — 3d was reserved from the start as conditional ("only if profiling warrants"). MEASURED instead of built: a throwaway benchmark timed the FULL `computeFrame` (scene walk + every boolean clip) over 60 frames on deliberately heavy synthetic scenes of ANIMATED group-of-rect operands. Results: 5 booleans/35 objs = **0.52ms/frame**, 20/180 = **1.77ms**, 50 booleans/550 objs (far beyond any real scene) = **7.37ms** — all under half the 16.7ms/60fps budget; a single heavy `resolveBooleanRings` (2 groups × 8 leaves) = 0.33ms. DECISION: do NOT build clip caching — a correct cache must key on each operand's RESOLVED geometry at `time` (operands animate) or invalidate on any operand transform/keyframe/parent change → broad invalidation surface + stale-clip correctness risk for ZERO measurable benefit (YAGNI). Re-open only if real-world profiling pushes `computeFrame` toward budget (dozens of deep-nested-boolean operands); lowest-risk first step = within-one-`computeFrame`-pass memoization before any cross-frame cache. **WITH 3d CLOSED, THE ANIMATED-BOOLEAN MILESTONE (1/2/3a/3b/3c/3d) IS COMPLETE.** | `specs/2026-06-28-savig-m4-animated-boolean-slice3d-clip-caching-decision.md` | (decision doc; no code) |
| **BOOLEAN — LIVE BOOLEAN INSIDE A GROUP OPERAND** — closes the 3b deferral: a live boolean nested in a GROUP operand was DROPPED (`operandWorldGeom`'s group branch mapped each `collectVectorLeaves` leaf through `objectToWorldPolygon`; a boolean child's empty fallback path → `[]`). FIX = route the per-leaf map through `operandWorldGeom` (dispatches `obj.boolean → resolveBooleanGeom`) + thread `visited`. ONE-LINE change. PARITY: plain-leaf groups byte-identical (`operandWorldGeom(plainLeaf) === objectToWorldPolygon(plainLeaf)`). Cycle-safe (threaded visited). `operandWorldRings`/export follow for free. SUBAGENT-DRIVEN (inline + sonnet review = Approved, 0 crit/imp); 1 TDD task. DEFERRED: a boolean's operand AND the boolean both children of the same group → double-counted in the union (benign for union). 1422 unit green. | `specs/2026-06-28-savig-m4-boolean-in-group-operand-design.md` · `plans/2026-06-28-savig-m4-boolean-in-group-operand.md` | (merge of 7b9672d) |
| **BOOLEAN — CURVE-PRESERVING GROUP OPERANDS** — extends the `f5d4477` leaf curve-preservation to GROUP operands: a group keeps its plain-vector leaves' curvature on untouched edges (a grouped circle stays curved through a boolean), union seams + clip intersections cornered. `booleanResultGeom` now pushes, for a group, ONE flat pre-union geom (unchanged group-as-one-operand semantics) PLUS one `OperandCubics` provenance entry PER plain-vector leaf — **each its own `opIdx`** (REQUIRED: a group's leaves form multiple disjoint rings; a shared opIdx would make reconstructRing's `verbatim` path concatenate them into a bogus ring). `reconstructRing`/`classifyVertex`/`tol` UNCHANGED — just more provenance operands. Boolean/nested-group/SVG leaves stay faceted (`operandCubicsWorld` → `[]`). PARITY: rects (straight cubics → corners), leaf operands, the existing "mixed group + leaf" test byte-identical. SUBAGENT-DRIVEN (inline + sonnet review = Approved, 0 crit/imp; 2 benign Minors); 1 TDD task. DEFERRED: nested-boolean operand curve preservation (its result has no original cubics); abutting-circle seam may keep a curve handle within tol (inherited seam-stitch approximation). 1425 unit green. | `specs/2026-06-28-savig-m4-boolean-curve-preserving-groups-design.md` · `plans/2026-06-28-savig-m4-boolean-curve-preserving-groups.md` | (merge of 5c760dd) |
| **BOOLEAN — SVG-ASSET OBJECTS AS OPERANDS** — a DIRECT SVG-asset object (`kind:'svg'`, raw markup) is now a boolean operand: its filled shapes' UNION (faceted) joins the clip, derived DETERMINISTICALLY from the markup via a pure-JS pipeline (jsdom + runtime-bundle safe — NO browser SVG geometry APIs). NEW `src/engine/geom/svg/`: **`parsePathD`** (SVG `d` → absolute command list; M/L/H/V/C/S/Q/T/A/Z abs+rel, S/T reflection, Z subpath-reset, packed `A` flags, malformed→partial-no-throw) + **`flattenSvg`** (`parseTransformList` matrix/translate/scale/rotate/skew; `flattenElementToRings` rect/circle/ellipse/polygon/path with De Casteljau cubic/quad + SVG-spec F.6.5 arc; `svgAssetRings` = DOMParser + viewBox→width/height base matrix + `<g transform>` recursion → object-local rings, never throws). ENGINE `boolean.ts`: `svgAssetOf` + an `operandWorldGeom` SVG branch (rings → `toWorld` → `pc.union`, faceted — flows the flat-geom path; `operandCubicsWorld` already `[]` for non-vector). AUTHORING: new `isSvgOperand` widens the store `eligible` filter + Inspector `eligibleForBool`; `vectorLeavesOf`/`hasVectorLeaf` stay VECTOR-ONLY (the style source); **BOTH style picks (live + destructive) GUARDED** — an all-SVG selection → `topLeaf` undefined → `DEFAULT_VECTOR_STYLE` (not `{...undefined}`). `vite.config`: `src/engine/geom/svg/** → jsdom` (DOMParser). RUNTIME BUNDLE REGENERATED (+15KB). v1 LIMITS: faceted (no curve provenance), DIRECT SVG only (SVG-in-group deferred — collectVectorLeaves is vector-only), even-odd holes fill solid, stroke ignored, no use/text/clip. PARITY: non-SVG booleans byte-identical (branch + eligibility OR-clause are additive). SUBAGENT-DRIVEN (parsePathD + flattenSvg per-task reviews CLEAN [0 crit/imp; flatten 2 Minors applied: scale-NaN guard + parsererror detect] + OPUS whole-branch = Ready-to-merge 0 crit/imp); 6 TDD tasks + e2e (`boolean-svg-operand.spec.ts`: import box.svg → disjoint rect → Union → ≥2 M subpaths). 1450 unit green. | `specs/2026-06-28-savig-m4-boolean-svg-operands-design.md` · `plans/2026-06-28-savig-m4-boolean-svg-operands.md` | (merge of 677c72d) |
| **SYMBOL CONTENT CLIPPING (slice 47e)** — opt-in hard clip for symbol instances: `SymbolAsset.clip?: boolean` (absent/false = no clip, byte-identical parity; true = every instance clips its content to `[0,width]×[0,height]` in symbol-local space). **ENGINE (`types.ts` + `symbol.ts`):** `InstanceLeaf` gains four optional fields: `clipId`, `clipTransform`, `clipWidth`, `clipHeight`. `flattenInstances.walk` threads a `clipCtx` param; when a symbol has `clip: true` and no enclosing clipCtx, establishes `{ clipId: 'clip-'+renderId, clipTransform: instTransform, clipWidth: asset.width, clipHeight: asset.height }` and propagates it down to every leaf in that symbol's subtree via object spread (v1: outermost clipping ancestor wins; nested clipping-in-clipping is deferred). **EXPORT (`renderDocument.ts`):** refactored into `renderLeaf` (per-leaf string) + `buildClipPathDefs` (clipPath defs from clipId dedupe) + main loop that groups consecutive same-clipId leaves under `<g clip-path="url(#clipId)">`. clipPath is `<clipPath id="..." clipPathUnits="userSpaceOnUse"><rect x="0" y="0" width="W" height="H" transform="instTransform"/>` — the clip rect carries the instance world transform so it's in the same coordinate space as the leaves. Gradient defs collected as a side effect. **STAGE (`Stage.tsx`):** `clipPathDefs` useMemo computes the same clipPath strings; concatenated to `dangerouslySetInnerHTML` defs. Render loop changed from `renderLeaves.map()` to an IIFE that groups clipping runs under `<g clipPath="url(#clipId)">` with `renderOneleaf` helper. **STORE:** `setSymbolClip(symId, clip)` action (spreads `clip: true` or `clip: undefined`; JSON.stringify drops undefined). **INSPECTOR:** "clip content" checkbox in the symbol-instance panel (after "symbol duration"). Consecutive-leaf grouping invariant documented: safe because flattenInstances processes each symbol's subtree depth-first. Runtime bundle unaffected (frame.ts only reads renderId/transformPrefix/opacityFactor/localTime). 1-PASS review CLEAN (0 Critical/Important); reviewer confirmed coordinate-space reasoning, consecutive-leaf invariant, parity, gradient side-effect correctness. Two findings addressed: inaccurate comment on `clip: undefined` (corrected) and missing invariant comment on grouping loops (added). 1468 unit + 25/25 symbols e2e green. DEFERRED: nested clipping (inner-clip-in-outer-clip adds another clipPath layer), animated clip bounds, per-instance clip region. | `specs/2026-06-28-savig-m4-symbol-clipping-design.md` · `plans/2026-06-28-savig-m4-symbol-clipping.md` | `7234045` |

| **PER-INSTANCE OVERRIDES: TINT + FIRST-FRAME (slice 47f)** — Two new optional fields on `SceneObject` let instances of the same symbol look different. **`freezeFirstFrame?: boolean`** forces the instance's internal clock to 0 (first frame / static poster) regardless of the playhead or any `symbolTime`/`symbolTimeTrack` remap — the guard is inserted before all existing remap logic in `flattenInstances` so it wins unconditionally; absent/false = animate normally (parity). **`tint?: { color: string; amount: number }`** applies an `feFlood + feComposite(in) + feBlend(multiply)` SVG filter to the entire composited group of the instance's leaves. `amount` controls `flood-opacity` (0 = no tint, 1 = full multiply). Filter region uses `x="-10%" y="-10%" width="120%" height="120%"` to accommodate stroke overflow. Absent = no filter (byte-identical parity). **ENGINE (`types.ts` + `symbol.ts`):** `InstanceLeaf` gains three optional fields: `tintId` (`savig-tint-<renderId>` prefix), `tintColor`, `tintAmount`. `walk()` gets a `tintCtx` param; when `o.tint` is present, a new `nextTintCtx` is established and propagated to all leaves in that instance's subtree. **EXPORT (`renderDocument.ts`):** main loop updated to detect `runTintId` (alongside `runClipId`); emits `<filter>` defs into `<defs>` and wraps tinted runs in `<g filter="url(#savig-tint-...)">` (outer); clip wrapper (inner) composes correctly when both apply. **STAGE (`Stage.tsx`):** `tintFilterDefs` useMemo builds filter strings from `renderLeaves`; included in `dangerouslySetInnerHTML` `<defs>`; leaf loop updated to wrap tinted runs in `<g filter>`. **STORE:** `setInstanceFreeze(freeze: boolean)` and `setInstanceTint(tint | undefined)` actions (clear field to `undefined` for clean JSON). **INSPECTOR:** "Instance overrides" section after symbol timing with freeze checkbox + tint checkbox + color input + amount number field. Runtime bundle regenerated (freeze changes `symbol.ts` which `frame.ts` bundles). 1 Important finding addressed by reviewer: filter region expanded from `0%/0%/100%/100%` to `-10%/-10%/120%/120%` to prevent stroke clipping. 1498 unit + 28/28 e2e green. DEFERRED: animated tint (color/amount keyframe tracks); tint blend mode selector; nested-tint composition semantics; SVG-asset-instance tint edge cases. | `specs/2026-06-28-savig-m4-per-instance-overrides-design.md` · `plans/2026-06-28-savig-m4-per-instance-overrides.md` | `1580e72` |

| **STATIC-SYMBOL `<use>` EXPORT OPTIMIZATION (slice 47g)** — when a symbol instance is fully static (zero effective animated duration, no per-instance animation overrides), the export now emits the symbol's content ONCE into `<defs>` as `<g id="savig-sym-<assetId>">…</g>` and renders each instance as a single `<use href="#savig-sym-<assetId>" transform="…" opacity="…"/>`, shrinking exports with repeated static symbols while staying visually identical. **ENGINE (`duration.ts`):** new `isStaticSymbol(asset, assetsById, visited?)` — true iff `symbolEffectiveDuration === 0` AND every nested symbol instance is recursively static (cycle-guarded); new `isStaticInstance(instance)` — true iff instance has no `symbolTimeTrack` (non-empty), no `symbolTime`, no `tint`, no `freezeFirstFrame` (conservative exclusions for v1). **EXPORT (`renderDocument.ts`):** `buildStaticOptimizableMap` pre-scans root `project.objects` for eligible instances (static symbol + static instance + no clip, computes instance world transform at t=0); `buildStaticSymbolDef` renders the symbol's own objects via new `renderSymbolObjects` (mirrors `flattenInstances` walk but at t=0, empty prefix, bare object ids as renderIds, recursively inlines nested symbol instances); CRITICAL correctness fix: passes `localProject = { ...project, objects: asset.objects }` so `resolveBooleanRings` resolves boolean operand ids in the SYMBOL-LOCAL scene (not root scene — otherwise a boolean object inside a static symbol would render as an empty path). Body loop pre-checks each leaf's top-level instance id against the static map: first encounter emits `<use>` + adds def; subsequent leaves of same instance skipped. Non-optimizable (animated, tinted, clipped, timed) instances fall through to existing body loop paths BYTE-IDENTICAL. Runtime bundle and editor Stage UNTOUCHED (static symbols carry no animation; runtime only touches animated leaves). 2-pass review: pass 1 = 1 Critical (boolean-in-static-symbol localProject bug, fixed) + 2 Important (addressed: boolean test added, SVG-asset-in-static-symbol test added, `isStaticSymbol` invariant comment added); pass 2 = CLEAN (0 Critical/0 Important). 1524 unit + e2e export.spec.ts green. v1 DEFERRALS: clipped static instances (clip+`<use>` composition); tinted static instances (tint+`<use>` composition); nested `<use>` within defs (nested instances are baked into the def's `<g>` at t=0). | `specs/2026-06-28-savig-m4-static-symbol-use-export-design.md` · `plans/2026-06-28-savig-m4-static-symbol-use-export.md` | `b3d4cb0` |

## Milestone 5 — Agent Authoring & Headless Render (IN PROGRESS)

Make Savig a first-class target for an AI agent producing animated shorts: expose its (already
pure) engine as a headless, id-addressed, scriptable surface; give the agent a *perceive→act* loop
(render frames it can see + introspect/validate); add a declarative DSL + semantic macros so an LLM
composes intent, not raw keyframes. Spec: `specs/2026-06-28-savig-m5-agent-authoring-design.md`.
Pulls forward + reshapes roadmap **M8 (video/GIF)** → render slice and **M9 (scripting)** → DSL/SDK,
plus a slice of **M7 (scenes)**.

**Architecture premise:** the engine is already pure & headless-capable (`createProject`,
`flattenInstances`, `computeFrame`, `computeProjectDuration`, `renderSvgDocument`, `saveSavig`).
The barrier was the selection-coupled Zustand store + no way for an agent to *see* output. M5
exposes the engine directly.

| # | Slice | Status | Merge |
|---|-------|--------|-------|
| **1** | **Headless authoring core** — `src/core`: deterministic `createIdFactory`; pure id-addressed builders (`addRect`/`addEllipse`/`addPath`/`setKeyframe`/`setBaseTransform`/`removeObjects`) returning a new `Project`; `describeProject(project)→string` (token-cheap summary); `validateProject(project)→Issue[]` (dangling refs / non-finite / off-artboard / past-duration / single-keyframe / symbol-cycle). Pure, store-free, FAILS-LOUD on bad refs; root-scene only. 20 new tests (1545 unit total). **group/createSymbol/placeSymbol DEFERRED** — they need a bbox-centre anchor and the geometry helpers (`objectAABB`/`sceneContentAABB`) currently live in the UI (`Stage/snapping.ts`); lifting them into the engine is slice 1b. | ✅ DONE | (this branch) |
| **2** | **Render-to-raster (the agent's eyes)** — `src/core/render.ts`: `renderFrameSvg(project,t)` bakes a static SVG at time `t` by rendering the real export markup (`renderSvgDocument`) and applying `applyFrameToNodes(computeFrame(t))` in jsdom — the SAME path the editor/runtime use, so the raster matches preview/export by construction; `renderFramePng`/`renderThumbnail`/`renderFrames(count)` rasterize via **@resvg/resvg-js** (added; jsdom promoted to a dep). 6 new tests (1551 unit total). Stitched sprite-sheet DEFERRED (one combined SVG would collide the export's def ids across cells) → `renderFrames` returns a `{time,png}[]` contact-sheet array. resvg caveat: SVG filter `tint` may not rasterize. | ✅ DONE | (this branch) |
| **3** | **Scene DSL + compiler** — `src/core/dsl.ts`: `ShortDoc` JSON schema (meta + rect/ellipse/path objects with style, static `base` overrides, and per-property `animate` keyframe tracks) → `compileShort(doc)→Project` over the slice-1 builders; `decompileProject(project)→ShortDoc` (best-effort inverse for the DSL-authorable subset; skips groups/symbols/svg/audio). Round-trip stable: `compile(decompile(compile(doc)))` is equivalent. Fails loud on malformed input. 6 new tests (1557 unit total). | ✅ DONE | (this branch) |
| **4** | **MCP server (end-to-end agent-driveable)** — `src/mcp`: a stdio MCP server (`@modelcontextprotocol/sdk`, low-level `Server` + JSON-Schema tool defs, no zod) holding one stateful `Session` (the in-progress Project). 12 tools: `new_short`/`load_dsl`/`add_rect`/`add_ellipse`/`set_keyframe`/`move_to`/`fade`/`describe`/`validate`/`render_frame`/`export_svg`/`get_dsl`. Mutating tools return describe + a thumbnail PNG (base64 image content) so the agent sees each edit. Tool LOGIC lives in a pure `tools.ts` table over `core` (unit-tested directly, 8 tests, 1573 total); `server.ts` is thin transport; `pnpm mcp` (tsx) runs it. Server construction smoke-tested. resvg/jsdom render in node; not bundled into the browser app. | ✅ DONE | (this branch) |
| **5** | **Animated GIF export** (absorbs roadmap M8) — `src/core/gif.ts`: `renderGif(project, opts)` renders frames to raw RGBA via resvg (`renderFrameRgba`, added) and encodes a looping GIF with the pure-JS **gifenc** (per-frame 256-colour quantize; frame `i` = `i/fps`s). New MCP tool `render_gif` (returns `image/gif`). gives an agent a self-playing artifact to watch. 3 new tests (1589 unit total). MP4 deferred (needs ffmpeg). | ✅ DONE | (this branch) |
| **6** | **Semantic animation macros** — `src/core/macros.ts`: `fadeIn`/`fadeOut`/`moveTo`/`scaleTo`/`rotateTo`/`spin`/`pulse`/`stagger` — verbs that expand to slice-1 `setKeyframe` calls (`from` defaults to the object's base). An agent composes intent, not cubic-bezier numbers. Pure, no deps. 8 new tests (1565 unit total). `moveAlongPath` deferred (needs the motion-path engine surface). | ✅ DONE | (this branch) |
| **7** | **Templates & example-shorts corpus** — `src/core/templates.ts`: 5 complete example shorts (`bouncing-ball`/`fade-in-title`/`staggered-dots`/`pulsing-badge`/`slide-and-spin`) built from the full stack (builders + macros), each a reproducible `build(): Project`. Doubles as few-shot material for the agent + a smoke test that the stack composes into valid, renderable shorts (12 tests: each validates clean, animates, renders). New MCP tools `list_templates`/`load_template`. 13 new tests (1586 unit total). | ✅ DONE | (this branch) |
| **8a** | **Camera** (animatable view transform — pan/zoom/roll, Ken-Burns) — `engine/camera.ts`: `Project.camera?` (pose = x/y/zoom/rotation, base + axis tracks; ABSENT = identity = parity); `computeCameraTransform` = `translate(W/2,H/2)·scale·rotate·translate(-x,-y)`. Wired into export (`renderSvgDocument` wraps the body in `<g data-savig-camera>`), the runtime + the headless `renderFrameSvg` via a shared `applyCamera` (runtime bundle regenerated, 136→154KB). Editor-preview camera DEFERRED (kept `Stage.tsx` untouched — zero editor regression risk; the agent's export/render paths fully animate the camera). Agent API: `setCamera`/`setCameraKeyframe`/`panTo`/`zoomTo`/`kenBurns` (core), `camera` DSL field (compile+decompile round-trip), `set_camera`/`camera_move` MCP tools. 16 new tests (1602 unit); e2e 106 + export parity green. Design spec: `specs/2026-06-28-savig-m5-slice8-scenes-camera-design.md`. | ✅ DONE | (this branch) |
| 8b | Scenes / multi-shot sequencing (the `Project`-model redesign) — restructures Project + the editor timeline. **DESIGN SPECCED** (`specs/2026-06-29-savig-m5-slice8b-multi-scene-sequencing-design.md`): `Project.scenes?` (absent = byte-identical parity via `projectScenes` accessor), `engine/scenes.ts` master-timeline (`resolveTimeline`/`sceneAtTime`/Σ-duration), scene-prefixed compute/export (global asset-keyed defs exempt), per-scene camera, scene strip + `selectedSceneId` routing. Decomposed into 6 sub-slices. **DONE & merged:** **8b-1a** (scene model `f8b26be`) · **8b-1b** (scene-aware `computeFrame` `24509f6`) · **8b-2a** (`renderSceneBody` extraction `d5bcfc6`) · **8b-2b** (`renderProjectDocument` `f90c050`) · **8b-2c** (runtime scene-switch + bundle regen + e2e `3ea3ce2`) · **8b-2d** (headless raster + describe `31eae8b`). The compute/export/runtime/raster half is COMPLETE — multi-scene shorts export, rasterize, and play in a browser (1646 unit + multi-scene e2e green). **REMAINING:** 8b-3 (editor — scene strip + `selectedSceneId` routing) · 8b-5 (DSL/MCP) · 8b-4 (transitions crossfade/dip). Plans: `plans/2026-06-29-savig-m5-slice8b-{1a,1b,2a,2b,2c,2d}-*.md`. Chose scene-scoped `Project`-view / post-flatten leaf-id-prefixing over the spec's flattenObjects/per-builder-idPrefix (also fixes the resolveBooleanRings + buildBundle seams). | 🚧 IN PROGRESS | 8b-1a/1b/2 done |
| **9** | **Text primitive** — new `TextAsset` kind (content/fontSize/fontFamily/fill/stroke/textAnchor); a `SceneObject` referencing it renders as SVG `<text>` (text-before-edge baseline). Additive: the union added cleanly (no exhaustive-switch breakage); `flattenInstances`/`computeFrame` already tolerate it (transform/opacity only, no geometry/runtime change). Wired the render branch into both `renderLeaf` (export, escaped) and the editor `Stage.renderOneleaf` (preview==export). `createTextAsset` (engine) + `addText` (core) + `text` DSL type (compile + decompile round-trip) + `add_text` MCP tool. 6 new tests (1592 unit); **e2e 106 green — editor unbroken**. v1: animates via transform/opacity (static content); no interactive text tool / in-symbol text yet. | ✅ DONE | (this branch) |

## What's next / backlog

Curated pointers — the authoritative lists live in each spec's *Deferred / Non-goals*
section and the master spec §10. When a slice ships, move it up into a table and prune here.

**GROUPING (45a–45f) + BOOLEAN OPS (46) ARE COMPLETE; NESTED-SYMBOLS (47a + 47b + 47-edit + 47c +
47d) ARE FULLY COMPLETE.** A group is a real container with its
own transform — create / select / move / scale / rotate / ungroup as a unit, **keyframe-ANIMATE**
it, **NEST** groups arbitrarily deep, **drag-REPARENT** members in the Layers tree (world-position
preserving). The transform composes onto children at compute time (no DOM node; preview==export);
the Layers panel shows the full hierarchy with a visibility cascade. `groupId` is gone. **Boolean
ops** (union/subtract/intersect/exclude) combine ≥2 vector shapes into one path, world-baking +
clipping via `polygon-clipping`, with `compoundRings` + `fill-rule:evenodd` for holes.
**Nested symbols:** a `SymbolAsset` (own `objects[]` scene) is instanced by an ordinary object's
`assetId`; the recursive `flattenInstances` walker expands instances onto a single compose path
(preview==export), cycle-guarded; `createSymbol` turns a selection into a reusable definition +
instance (47a). A selected instance has a full TRANSFORM UI — bbox + scale/rotate handles
(`instanceAABB`), move-snapping, surgical live drag-preview — generalized from the group chrome,
autoKey-gated like any object (47b). And you can **EDIT a symbol IN PLACE** — double-click an
instance to descend into its scene (`editPath` "focused-scene" view: Stage/Timeline/Layers/Inspector
all scope to the symbol's `objects`; transform/animate its parts → every instance updates;
breadcrumb/Esc to exit), with undo/persistence free and parity untouched (47-edit). And each instance
can run its **own internal timeline** — a `symbolTime` (start offset / loop / speed) remaps the
`flattenInstances` `localTime` seam so two instances of one symbol show **different frames** at the
same playhead (absent = identity → parity-safe; export-remap free) (47c). And a **symbols LIBRARY**
(AssetPanel "Symbols" section + Inspector swap) lets you browse symbols with a live instance count,
**place** a fresh instance without a prior selection, and **swap** an instance to another symbol — all
gated by the authoring-time `symbolContains` **cycle guard #2** (47d). **All M4 headline features —
multi-object, grouping, boolean, and the full nested-symbols arc — are now shipped.** What remains is
polish. **The entire IN-SYMBOL EDITING SURFACE is now CLOSED** — "author inside a symbol" (phases 1–9:
delete/draw/node-edit/paint/layers/clipboard/group-boolean/motion/morph) PLUS the general in-symbol
timeline keyframe editing (remove/retime/copy/paste/easing/rotation for every track type) all route to the
active scene. Every editing action that operates on a selected object or keyframe works inside a symbol.
What remains is bounded symbol-library / grouping polish.

**✅ M4 IS COMPLETE.** Every headline feature (multi-object, grouping, boolean ops + the full animated-boolean milestone + all boolean follow-ups, the entire nested-symbols arc incl. in-symbol editing) AND all the polish (snapping family, layout, clipboard/duplicate group semantics, symbol clipping/overrides/export-opt) has shipped. The only open items are PRINCIPLED DEFERRALS (animated-group ungroup = transform-representability limit; per-frame clip caching = measured-not-warranted; nested/SVG-operand curve preservation) and NOT-PURSUED dubious items (spacing-for-scale). Remaining backlog below is NON-M4 (future milestones). The priority table below is fully struck-through (historical).

**Recommended next (M4):** all priority items shipped — see git history. The full
struck-through priority table was removed in the 2026-06-28 consolidation (provenance
lives in each slice's row above + the `plans/` files). Nothing M4 remains open except the
principled deferrals below.

### Carryover backlog (optional — none blocks M5)

Consolidated 2026-06-28. Split into what we've **chosen not to do** (representability /
cost-benefit limits) vs. **optional polish** a future milestone could pick up. None of this
gates starting M5 (CSS export).

**Principled deferrals (decided NOT to pursue):**

| Item | Why deferred |
|------|--------------|
| Exact ungroup of an ANIMATED or non-uniformly-rotated (sheared) group | Can't bake an animated transform / shear into a static `Transform2D`; the common identity/translate case is already exact |
| Curve preservation for NESTED-boolean results + SVG-asset operands; even-odd holes in an SVG operand | v1 boolean limits; leaf + group operands already preserve cubics (`f5d4477`/`5c760dd`) |
| Per-frame boolean clip caching | Profiled (slice 3d) — measured, not warranted |
| Equal-spacing snap for SCALE drags | Spacing is a positioning concept, not a sizing one — no clear semantic; scale handles already object/grid/constrained-grid snap |

**Optional polish (could pick up anytime; low value vs. M5):**

| Candidate | Source |
|-----------|--------|
| Cross-OBJECT keyframe paste; multi-keyframe select + cut | slice29 / slice24 deferrals |
| Cross-TYPE gradient morph (linear↔radial); userSpaceOnUse / spreadMethod / gradientTransform; per-stop opacity UI; HSL/OKLCH stops; `fill: string \| Gradient` paint-union refactor | slice34 §2, slice8/9 deferrals |
| Parametric LINE/rect/ellipse; animating primitive params; on-canvas radius/rotation handle | slice35 §4 |
| Pressure/velocity-variable brush width; input stabilizer; Schneider least-squares fit; textured brushes | slice7 §13 |
| Alt-to-scale-from-CENTRE for rotate; aspect-ratio snapping presets | slice28/30 deferrals |
| Bundled starter project; single-file vs folder export; license choice | master §10 C-list |

**Internal health follow-ups (from the 2026-06-28 code-review — status after the refactor pass):**

| Item | Status |
|------|--------|
| `pnpm lint` scanned the repo root and failed on `.remember/tmp/*.ts` | ✅ DONE — added `.remember`/`test-results`/`playwright-report` to `eslint.config.js` `ignores`; lint green |
| `store.ts` god-store (was 2545 LOC, ~120 actions) | ✅ SPLIT — now 1484 LOC + `store-internals.ts` (types/helpers/constants) + `slices/groupSymbolSlice.ts` (grouping/symbols/boolean) + `slices/transportPrefsSlice.ts` (transport/view/prefs/toasts), composed via the Zustand slice pattern (`SliceCreator`); public surface unchanged. Remaining keyframe/draw/transform domains still in core — same pattern applies if wanted |
| 7 parallel `selected*Keyframe` select/clear blocks | ✅ DEDUPED — the ~50 lines of repeated "null the other six" now share one `NO_KEYFRAME_SELECTION` patch. (Kept the 7 public fields rather than a discriminated union, to avoid rewriting the tests that validate them — chosen over the union per 2026-06-28 review discussion.) |
| `Stage.tsx` (2592 → 1696 LOC, −35%) | ✅ DONE — **every pointer interaction** extracted into a focused hook/module via a **delegation pattern** (hook owns its ref/state; Stage keeps its single window move/up listener pair and calls `beginX`/`move`/`end`, each returning true when it consumed the event → event ordering byte-identical; the interactions are mutually exclusive so call-order is immaterial): `stageCoords.ts`, `usePanZoom`, `useMarqueeSelect`, `useDrawTool`, `useBrushTool`, `useGradientDrag`, `useRotateDrag` (single + group rotate), `useScaleDrag` (scale + resize + group-scale), `useObjectDrag` (single + multi move), `useNodeDrag` (node-anchor drag + snap; path-edit state stays in `usePathTools`). The transform core was extracted by building each hook's shared-deps ctx ONCE **inside the `[]` pointer effect**, so the mount-capture semantics (notably `zoom`) match the old inline branches exactly. Each step verified 1525 unit + 106 e2e; all merged to main. What's left in Stage.tsx is JSX + derived memos + the pointer-down snapshot handlers — no inline drag state machine |

**Roadmap docs:** big-picture M1–M11 table + C-list →
`specs/2026-06-19-savig-animated-svg-editor-design.md` §10; M2 morph/easing sequencing →
`specs/2026-06-20-savig-m2-morph-easing-roadmap-design.md`.
